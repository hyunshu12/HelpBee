# YOLO 학습용 의존성. 추론 서버는 requirements.txt 사용.
# pip install -r requirements-gpu.txt
#
# torch 핀 (학습 박스 RTX 4060 / CUDA 12.1 휠 — 별도 index 라 이 파일에 핀하지 않는다):
#   torch==2.4.1+cu121 / torchvision==0.19.1
#   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu121
#   → 그 다음 pip install -r requirements-gpu.txt  (아래 범위가 설치된 torch 를 만족하므로 재설치 안 함)
-r requirements.txt

# Deep learning (CUDA 12.1 휠. CPU only면 torch만 따로 깔 것)
torch>=2.2,<2.5
torchvision>=0.17,<0.20

# YOLO
ultralytics>=8.3,<8.4

# Augmentation
albumentations>=1.4,<2.0

# Experiment tracking
wandb>=0.16

# Data tools
roboflow>=1.1
pandas>=2.1
scikit-learn>=1.4
tqdm>=4.66

# Small object inference
sahi>=0.11

# Annotation conversion
pycocotools>=2.0

# YAML
pyyaml>=6.0

# ONNX export/검증 + 통계(보정·CI)
onnx>=1.16
onnxruntime>=1.18
scipy>=1.11

# numpy 핀은 requirements.txt(단일 소스)에서 상속 — numpy>=1.24,<2.0
