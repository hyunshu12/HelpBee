[pytest]
pythonpath = .
testpaths = app/tests
asyncio_mode = auto
# 기본 실행(pytest -q)은 회귀 게이트 제외 — 서빙 추론이 무겁고 로컬 데이터셋/모델 의존.
# 명시 선택 `pytest -q -m regression` 은 CLI -m 이 이 기본값을 덮어써 회귀만 돈다.
addopts = -m "not regression"
markers =
    regression: 서빙 경로 스냅샷 회귀 게이트 (모델+71667 Sample 있을 때만; 없으면 skip)
