{
  "name": "@helpbee/database",
  "version": "0.1.0",
  "private": true,
  "description": "Database schema and migrations using Drizzle ORM",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "migrate": "drizzle-kit migrate",
    "push": "drizzle-kit push:pg",
    "studio": "drizzle-kit studio",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.28.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "drizzle-kit": "^0.20.0"
  }
}
