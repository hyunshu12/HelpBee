# 부스 진단 투어 v2 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 부스 체험을 사진 4장 자유선택(40~60초)에서 난이도 계단이 있는 3장 진단 투어(약 2분 30초)로 바꾸고, 병 세 종류를 보여준다.

**Architecture:** 기존 화면(추측→분석→결과)을 라운드 루프로 3번 돌리고, 앞뒤에 투어 시작·요약 화면을 붙인다. 케이스 데이터에 `kind`(visible/varroa/healthy)를 추가해 라운드 배정 키로 쓴다. 배정은 순수 함수로 떼어 테스트한다. 화면 재구성이 아니라 배선 변경 + 화면 2개 추가 + 화면 2개 삭제다.

**Tech Stack:** Flutter 3.38.6 / Dart 3.10 (외부 pub 패키지 0개) · Python 3.10 + Pillow (생성기)

**Spec:** `plans/2026-09-08_부스투어-설계.md`

## Global Constraints

- **외부 pub 패키지 추가 금지.** `apps/booth/pubspec.yaml` 의 `dependencies:` 는 `flutter` 하나뿐이어야 한다 (같은 소스로 iOS·웹 빌드).
- **AI Hub 71667 원본 재배포 금지.** `assets/photos/`, `assets/varroa_closeup.jpg`, `assets/cases.json` 은 `.gitignore` 에 있다 — 절대 커밋하지 않는다.
- **bbox 는 `[x1,y1,x2,y2]`(xyxy).** `make_booth_cases.py` 가 `x2-x1`, `y2-y1` 로 w/h 를 만든다. xywh 로 "고치지" 말 것.
- **부스 캔버스 1366×1024 가로 고정.** 새 화면도 `layout_overflow_test` 스윕(1366×1024 · 1280×720)을 통과해야 한다.
- **번들 서체만 사용.** 본문 `kBodyFont`(NotoSansKR), 표시용 `kDisplayFont`(Jua). `TextPainter` 등 위젯 트리 밖 텍스트는 서체를 명시해야 한다 — 웹에서 두부(□)가 된다.
- **새 한글 문구를 추가하면 `python3 tool/build_fonts.py` 재실행 필요** (서브셋에 글자가 없으면 Jua→Noto 폴백, 최악에도 두부는 아님). 8단계에서 한 번에 처리한다.
- **파일 삭제는 `rm` 금지 → `trash <path>`.**
- 브랜치: `feature/booth-tour-v2` → PR → `develop`.

## 설계 문서 정정 (구현 중 발견, 이 계획이 우선)

1. **`kind=visible` 의 recommendations 를 새로 쓰지 않는다.** `app/services/risk.py:compute_risk()` 가 `CLASS_OTHER` 카운트를 받으면 `risk.yaml` 의 `recommendations.other_disease` 3줄을 이미 붙인다. 제품 문구를 그대로 쓰는 게 설계 원칙(생성기 docstring)에 맞다. 설계 §3 의 "생성기에 병별 정적 문구"는 폐기.
2. **`kind=visible` 에는 티어 배지도 그리지 않는다.** 티어는 *응애* 감염률 개념이라 DWV 벌통은 `compute_risk` 상 `safe` 가 나온다 — 병든 벌통에 초록 "안전" 배지가 붙는다. 설계 §3 의 "부스 전용 tier 규칙"은 폐기하고, visible 은 **병 이름 + 개체 수만** 보여준다. 게이지·배지 없음.
3. **`BoothCase.isHealthy` 는 버그가 된다.** 지금은 `tier == safe` 인데, visible 케이스도 `safe` 라 "건강함" 추측이 정답 처리된다. `kind == healthy` 기준으로 바꾼다 (Task 2).

---

## 파일 구조

**생성 (Dart)**
| 파일 | 책임 |
|---|---|
| `lib/data/case_kind.dart` | `CaseKind` enum + JSON 파싱. `BoothCase` 가 의존 |
| `lib/data/tour.dart` | 라운드 배정 순수 함수 + `TourRound` · `TourResult` 값 객체 |
| `lib/screens/tour_start_screen.dart` | 투어 시작 안내 |
| `lib/screens/summary_screen.dart` | 3장 결과 요약 |
| `lib/widgets/round_indicator.dart` | `1 / 3` 표시 |

**수정 (Dart)**
| 파일 | 변경 |
|---|---|
| `lib/data/booth_case.dart` | `kind`·`disease`·`diseaseLabel`·`sickCount` 추가, `varroaCount` 제거, `isHealthy` 수정 |
| `lib/data/booth_session.dart` | 추이 그래프용 상태 제거 → 투어 라운드 상태 |
| `lib/main.dart` | `BoothStage` 개편 + 라운드 루프 배선 + `AnimatedSwitcher` |
| `lib/screens/report_screen.dart` | kind 3분기 + 대조 문구 7종 + 버튼 정리 |
| `lib/screens/guess_screen.dart` · `analyzing_screen.dart` | 라운드 표시, 분석 4초 |
| `lib/screens/picker_screen.dart` | 10장 5×2 |
| `lib/widgets/bbox_overlay.dart` | 직각 + 라벨 태그 + 감속 |
| `lib/widgets/booth_scaffold.dart` | 벌집 배경·그라디언트·eyebrow 제거 |
| `lib/theme/app_colors.dart` | `boxDisease` 추가 |

**삭제 (Dart)** — `lib/screens/history_screen.dart`, `lib/widgets/trend_chart.dart`, `test/trend_chart_test.dart`

**수정 (Python)** — `apps/ai/training/data/make_booth_cases.py`, `apps/ai/app/tests/unit/test_make_booth_cases.py`

---

## Task 1: 생성기 확장 — kind/disease/sickCount + 신규 사진

**Files:**
- Modify: `apps/ai/training/data/make_booth_cases.py`
- Test: `apps/ai/app/tests/unit/test_make_booth_cases.py`

**Interfaces:**
- Consumes: `app.services.risk.compute_risk`, `CLASS_VARROA`, `CLASS_OTHER`, `CLASS_NORMAL`; `training.data.aihub_to_yolo.CLASS_MAPPING`
- Produces: `cases.json` 의 케이스마다 `kind: "visible"|"varroa"|"healthy"`, `disease: "dwv"|"chalkbrood"|"varroa"|null`, `diseaseLabel: str|null`, `sickCount: int`, `boxes[].cls: "varroa"|"disease"|"normal"`. `varroaCount` 키는 사라진다.

- [x] **Step 1: 사진 10장 선정 — 완료 (컨트롤러가 렌더해서 눈으로 확정)**

박스를 그려 렌더한 뒤 직접 보고 골랐다. 결과는 Step 4 의 `BOOTH_CASES` 에 경로까지
확정돼 있으니 **다시 고르지 말 것.** 판단 근거만 남긴다:

| id | 감염/전체 | 왜 이걸 골랐나 |
|---|---|---|
| chalk-1 | 15/17 | 하얗게 굳은 애벌레가 검은 소방과 대비돼 **누가 봐도 이상하다**. R1 의 표준 |
| chalk-2 | 12/12 | 전부 감염 — 밀도가 chalk-1 과 달라 반복 관람객에게 다른 그림 |
| dwv-1 | 4/9 | 벌통 사진 중 날개불구가 가장 많이 잡힌 것. 다만 변형이 육안으로 약해 R1 중 가장 어렵다 |
| danger-100 / -90 | 2/7, 1/6 | 기존 검증분 |
| danger-2 | 2/8 | 응애 박스 2개가 크게 잡혀 결과 화면 연출이 강하다 |
| watch-50 / -2 | 1/14, 1/16 | 16마리 중 1마리 — R2 보다 더 미세하다 |
| safe-0 / -2 | 0/6, 0/18 | **응애 0 · 다른 병 0** 인 것만. `성충_정상` 폴더에도 감염 개체가 섞인 사진이 많다(폴더명 ≠ 라벨) |

**날개불구를 R1 주력으로 쓰지 않은 이유**: 데이터셋의 날개불구는 (1) 벌통 사진은
날개 변형이 육안으로 거의 안 보이고, (2) 크게 찍힌 것은 **흰 배경의 실험실 표본
사진**이라 "이 벌통, 건강해 보이나요?" 와 맞지 않는다. 그래서 visible 풀을
석고병 2 + 날개불구 1 로 구성해 **R1 이 2/3 확률로 석고병**이 되게 했다.

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`apps/ai/app/tests/unit/test_make_booth_cases.py` 끝에 추가:

```python
def test_visible_case_has_disease_fields():
    """다른 병 케이스는 병 이름을 달고, 응애 카운트가 아니라 병 개체 수를 센다."""
    from training.data.make_booth_cases import BoothCaseSpec, build_case

    spec = BoothCaseSpec(
        id="dwv-1",
        rel="성충/성충_날개불구바이러스감염증/015/B_001_001_20230824130702_001_004_001_002",
        brightness=1.25,
        contrast=1.15,
        kind="visible",
        disease="dwv",
        disease_label="날개불구 바이러스",
    )
    case = build_case(spec)

    assert case["kind"] == "visible"
    assert case["disease"] == "dwv"
    assert case["diseaseLabel"] == "날개불구 바이러스"
    assert case["sickCount"] >= 2, "감염 개체가 2마리 이상인 사진을 골라야 한다"
    assert "varroaCount" not in case, "varroaCount 는 sickCount 로 대체됐다"
    # 다른 병 박스는 'disease' 로 나가야 결과 화면이 주황으로 그린다.
    assert any(b["cls"] == "disease" for b in case["boxes"])
    assert not any(b["cls"] == "varroa" for b in case["boxes"])
    # 처방은 제품 코드(risk.yaml)에서 온다 — 부스에서 지어내지 않는다.
    assert any("다른 질병" in r for r in case["recommendations"])


def test_varroa_case_still_counts_varroa():
    from training.data.make_booth_cases import BOOTH_CASES, build_case

    spec = next(s for s in BOOTH_CASES if s.kind == "varroa")
    case = build_case(spec)
    assert case["kind"] == "varroa"
    assert case["disease"] == "varroa"
    assert case["sickCount"] >= 1
    assert any(b["cls"] == "varroa" for b in case["boxes"])


def test_healthy_case_has_no_sick_boxes():
    from training.data.make_booth_cases import BOOTH_CASES, build_case

    spec = next(s for s in BOOTH_CASES if s.kind == "healthy")
    case = build_case(spec)
    assert case["kind"] == "healthy"
    assert case["disease"] is None
    assert case["sickCount"] == 0
    assert all(b["cls"] == "normal" for b in case["boxes"])


def test_pool_covers_every_round():
    """라운드 배정이 성립하려면 세 종류가 모두 있어야 한다."""
    from training.data.make_booth_cases import BOOTH_CASES

    kinds = [s.kind for s in BOOTH_CASES]
    assert kinds.count("visible") >= 3
    assert sum(1 for s in BOOTH_CASES if s.kind == "varroa") >= 5
    assert kinds.count("healthy") >= 2
    assert len(BOOTH_CASES) == 10
    assert len({s.id for s in BOOTH_CASES}) == 10, "id 중복"
```

- [ ] **Step 3: 실패를 확인한다**

Run: `cd apps/ai && ./.venv/bin/python -m pytest app/tests/unit/test_make_booth_cases.py -q`
Expected: FAIL — `BoothCaseSpec.__init__() got an unexpected keyword argument 'kind'`

- [ ] **Step 4: 생성기를 고친다**

`make_booth_cases.py` 의 `BoothCaseSpec` 과 `BOOTH_CASES` 를 교체:

```python
@dataclass(frozen=True)
class BoothCaseSpec:
    id: str
    rel: str          # 01.원천데이터 / 02.라벨링데이터 공통 상대경로 (확장자 없음)
    brightness: float  # 표시용 보정 — 원본이 어두워 아이패드에서 안 보인다
    contrast: float
    kind: str          # "visible" | "varroa" | "healthy" — 라운드 배정 키
    disease: str | None       # "dwv" | "chalkbrood" | "varroa" | None
    disease_label: str | None  # 화면 표시명


# 라운드 배정(설계 §2)이 성립하려면 세 kind 가 모두 있어야 한다:
#   R1 visible(쉬움) / R2 varroa danger(어려움) / R3 varroa watch | healthy(더 어려움)
BOOTH_CASES: list[BoothCaseSpec] = [
    # ── R1 후보: 눈에 보이는 병 (하얗게 굳은 애벌레는 누가 봐도 이상하다)
    BoothCaseSpec("chalk-1", "유충/유충_석고병/044/B_001_001_20230819135429_001_004_000_002",
                  1.35, 1.20, "visible", "chalkbrood", "석고병"),
    BoothCaseSpec("chalk-2", "유충/유충_석고병/032/B_001_001_20230819135415_001_004_000_002",
                  1.35, 1.20, "visible", "chalkbrood", "석고병"),
    BoothCaseSpec("dwv-1", "성충/성충_날개불구바이러스감염증/015/B_001_001_20230824130702_001_004_001_002",
                  1.25, 1.15, "visible", "dwv", "날개불구 바이러스"),
    # ── R2 후보: 응애 위험 (전부 응애 2마리 이상, 박스가 크게 잡힌 것)
    BoothCaseSpec("danger-100", "성충/성충_응애/082/A_001_001_20230822060110_011_001_001_001",
                  1.25, 1.15, "varroa", "varroa", "응애"),
    BoothCaseSpec("danger-90", "성충/성충_응애/089/A_001_001_20230822060113_007_001_001_001",
                  1.25, 1.15, "varroa", "varroa", "응애"),
    BoothCaseSpec("danger-2", "성충/성충_응애/057/B_001_006_20230820112138_001_003_001_001",
                  1.25, 1.15, "varroa", "varroa", "응애"),
    # ── R3 후보: 응애 주의 (16마리 중 1마리급 — R2보다 더 미세하다)
    BoothCaseSpec("watch-50", "성충/성충_응애/033/B_001_003_20230824081648_001_003_001_001",
                  1.55, 1.25, "varroa", "varroa", "응애"),
    BoothCaseSpec("watch-2", "성충/성충_응애/007/B_001_001_20230822083841_001_002_001_001",
                  1.55, 1.25, "varroa", "varroa", "응애"),
    # ── R3 후보: 정상(함정) — 응애 0 · 다른 병 0 인 것만
    BoothCaseSpec("safe-0", "성충/성충_정상/005/B_001_001_20230819135627_001_003_001_000",
                  1.50, 1.25, "healthy", None, None),
    BoothCaseSpec("safe-2", "성충/성충_정상/057/B_001_001_20230822083827_001_004_001_000",
                  1.50, 1.25, "healthy", None, None),
]
```

`build_case()` 의 박스 생성과 반환 dict 를 교체 (기존 xyxy 주석 블록은 그대로 둔다):

```python
        x1, y1, x2, y2 = (float(v) for v in ann["bbox"])
        boxes.append({
            "x": x1,
            "y": y1,
            "w": x2 - x1,
            "h": y2 - y1,
            # 3-class → 화면 색. varroa=빨강 / disease=주황 / normal=초록
            "cls": {CLASS_VARROA: "varroa", CLASS_OTHER: "disease"}.get(cls, "normal"),
        })

    risk = compute_risk(counts)
    # 응애든 다른 병이든 "이상 개체 수" 하나로 셈한다 — 화면 문구가 하나면 된다.
    sick = counts[CLASS_VARROA] + counts[CLASS_OTHER]
    return {
        "id": spec.id,
        "photo": f"photos/{spec.id}.jpg",
        "kind": spec.kind,
        "disease": spec.disease,
        "diseaseLabel": spec.disease_label,
        "imageWidth": width,
        "imageHeight": height,
        "riskScore": risk.risk_score,
        "tier": risk.tier,
        "beeTotal": risk.bee_total,
        "sickCount": sick,
        # 처방은 제품 코드(compute_risk → risk.yaml)에서 온다. 다른 병 케이스는
        # CLASS_OTHER 카운트 때문에 other_disease 문구가 자동으로 붙는다 —
        # 부스에서 문구를 지어내지 않는다.
        "recommendations": list(risk.recommendations),
        "boxes": boxes,
    }
```

파일 상단 import 에 `CLASS_OTHER` 를 추가한다:

```python
from app.services.risk import CLASS_OTHER, CLASS_VARROA, compute_risk
```

- [ ] **Step 5: 통과를 확인한다**

Run: `cd apps/ai && ./.venv/bin/python -m pytest app/tests/unit/test_make_booth_cases.py -q`
Expected: PASS

- [ ] **Step 6: cases.json 과 사진을 다시 굽는다**

```bash
cd apps/ai && ./.venv/bin/python -m training.data.make_booth_cases
ls ../booth/assets/photos/   # 10장이어야 한다
./.venv/bin/python -c "
import json; d=json.load(open('../booth/assets/cases.json'))['cases']
print(len(d), '케이스')
for c in d: print(f\"{c['id']:<12} {c['kind']:<8} sick={c['sickCount']:<3} bee={c['beeTotal']:<3} {c['tier']}\")"
```

기대: 10장, `kind` 가 visible 3 / varroa 5 / healthy 2.

- [ ] **Step 7: 커밋**

```bash
cd /Users/hyeonsyu/Documents/02_Work/04_MRMR/01_HelpBee
git add apps/ai/training/data/make_booth_cases.py apps/ai/app/tests/unit/test_make_booth_cases.py
git commit -m "feat(booth-data): 케이스에 kind/disease/sickCount 추가, 사진 10장으로 확장"
```

> `apps/booth/assets/` 산출물은 `.gitignore` 대상이라 커밋되지 않는다 — 정상이다.

---

## Task 2: Dart 데이터 모델

**Files:**
- Create: `apps/booth/lib/data/case_kind.dart`
- Modify: `apps/booth/lib/data/booth_case.dart`
- Test: `apps/booth/test/booth_case_test.dart`

**Interfaces:**
- Consumes: Task 1 의 `cases.json` 스키마
- Produces: `enum CaseKind { visible, varroa, healthy }`, `CaseKind caseKindFromName(String?)`; `BoothCase.kind`, `.disease`, `.diseaseLabel`, `.sickCount`, `.isHealthy`(=`kind == CaseKind.healthy`). `BoothCase.varroaCount` 는 **없어진다** — 쓰던 곳은 Task 5 에서 고친다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/booth/test/booth_case_test.dart` 끝(마지막 `}` 앞)에 추가:

```dart
  test('kind 를 읽고, 건강 여부는 tier 가 아니라 kind 로 판단한다', () {
    // 회귀: 예전 isHealthy 는 tier == safe 였다. 다른 병 케이스는 응애가 0이라
    // compute_risk 상 tier=safe 가 나오므로, 병든 벌통에 '건강함' 추측이
    // 정답 처리되는 버그가 있었다.
    final visible = BoothCase.fromJson({
      'id': 'dwv-1',
      'photo': 'photos/dwv-1.jpg',
      'kind': 'visible',
      'disease': 'dwv',
      'diseaseLabel': '날개불구 바이러스',
      'imageWidth': 1920,
      'imageHeight': 1080,
      'riskScore': 0,
      'tier': 'safe',
      'beeTotal': 9,
      'sickCount': 4,
      'recommendations': <String>[],
      'boxes': <dynamic>[],
    });

    expect(visible.kind, CaseKind.visible);
    expect(visible.diseaseLabel, '날개불구 바이러스');
    expect(visible.sickCount, 4);
    expect(
      visible.isHealthy,
      isFalse,
      reason: 'tier 가 safe 여도 병든 벌통이다 — 건강함으로 치면 안 된다',
    );
  });

  test('정상 케이스만 isHealthy 다', () {
    final healthy = BoothCase.fromJson({
      'id': 'safe-0',
      'photo': 'photos/safe-0.jpg',
      'kind': 'healthy',
      'disease': null,
      'diseaseLabel': null,
      'imageWidth': 1920,
      'imageHeight': 1080,
      'riskScore': 0,
      'tier': 'safe',
      'beeTotal': 6,
      'sickCount': 0,
      'recommendations': <String>[],
      'boxes': <dynamic>[],
    });
    expect(healthy.isHealthy, isTrue);
    expect(healthy.disease, isNull);
  });

  test('모르는 kind 는 varroa 로 떨어지지 않고 healthy 로도 가지 않는다', () {
    expect(caseKindFromName('nonsense'), CaseKind.varroa);
    expect(caseKindFromName(null), CaseKind.varroa);
  });
```

파일 상단 import 에 `import 'package:helpbee_booth/data/case_kind.dart';` 를 추가한다.

- [ ] **Step 2: 실패를 확인한다**

Run: `cd apps/booth && flutter test test/booth_case_test.dart`
Expected: 컴파일 실패 — `Undefined name 'CaseKind'`

- [ ] **Step 3: `case_kind.dart` 를 만든다**

```dart
/// 케이스의 성격. **라운드 배정의 유일한 키**이자, 결과 화면이 무엇을
/// 그릴지 결정하는 값이다 (설계 §2·§4.4).
///
/// - [visible]  눈에 보이는 병 (날개불구·석고병) — R1, 관람객이 맞히기 쉽다
/// - [varroa]   응애 — R2/R3, 알고 봐도 안 보인다
/// - [healthy]  정상 — R3 의 함정
///
/// tier 로 대신할 수 없다: 다른 병 케이스는 응애가 0이라 `compute_risk` 가
/// tier=safe 를 준다. tier 는 *응애* 위험도라 병 종류를 구분하지 못한다.
enum CaseKind { visible, varroa, healthy }

/// cases.json 의 kind 문자열 → enum.
///
/// 모르는 값은 [CaseKind.varroa] 로 떨어뜨린다 — healthy 로 잘못 떨어지면
/// 병든 벌통을 "정답: 건강함"으로 채점하게 되므로, 틀리더라도 안전한 쪽으로.
CaseKind caseKindFromName(String? name) => switch (name) {
  'visible' => CaseKind.visible,
  'healthy' => CaseKind.healthy,
  _ => CaseKind.varroa,
};
```

- [ ] **Step 4: `booth_case.dart` 를 고친다**

`BoothBox` 의 doc comment 를 갱신하고 기본값을 바꾼다:

```dart
/// 이미지 원본 픽셀 좌표계의 박스 하나.
///
/// `cls` 는 `'varroa'`(응애 의심) · `'disease'`(다른 병 의심) · `'normal'`(그 외 벌).
/// 결과 화면이 각각 빨강 · 주황 · 초록으로 그린다.
class BoothBox {
```

```dart
    cls: json['cls'] as String? ?? 'normal',
```

`BoothCase` 에서 `varroaCount` 를 지우고 아래를 추가한다 (생성자·필드·`fromJson` 세 곳 모두):

```dart
  final CaseKind kind;
  final String? disease;
  final String? diseaseLabel;

  /// 이상 개체 수 — 응애든 다른 병이든 하나로 센다.
  final int sickCount;

  /// 관람객이 '건강함'이라고 답했을 때 맞은 것으로 칠지.
  ///
  /// **tier 가 아니라 kind 로 판단한다.** 다른 병 케이스는 응애가 0이라
  /// tier=safe 가 나오는데, 그걸 건강하다고 치면 병든 벌통에 '건강함' 추측이
  /// 정답이 된다.
  bool get isHealthy => kind == CaseKind.healthy;
```

`fromJson` 에 추가:

```dart
    kind: caseKindFromName(json['kind'] as String?),
    disease: json['disease'] as String?,
    diseaseLabel: json['diseaseLabel'] as String?,
    sickCount: (json['sickCount'] as num).toInt(),
```

- [ ] **Step 5: 통과를 확인한다**

Run: `cd apps/booth && flutter test test/booth_case_test.dart`
Expected: PASS. (다른 테스트 파일은 아직 `varroaCount` 를 써서 깨진다 — Task 5 까지 정상이다.)

- [ ] **Step 6: 뮤테이션으로 테스트가 진짜 잡는지 확인한다**

`isHealthy` 를 `tier == RiskTier.safe` 로 되돌린 뒤 `flutter test test/booth_case_test.dart` 를 돌려 "건강함으로 치면 안 된다" 가 실패하는지 본다. 확인 후 되돌린다.

- [ ] **Step 7: 커밋**

```bash
git add apps/booth/lib/data/case_kind.dart apps/booth/lib/data/booth_case.dart apps/booth/test/booth_case_test.dart
git commit -m "feat(booth): CaseKind 도입, isHealthy 를 tier 대신 kind 기준으로"
```

---

## Task 3: 라운드 배정 + 투어 세션

**Files:**
- Create: `apps/booth/lib/data/tour.dart`
- Modify: `apps/booth/lib/data/booth_session.dart`
- Test: `apps/booth/test/tour_test.dart` (신규), `apps/booth/test/booth_session_test.dart`

**Interfaces:**
- Consumes: `BoothCase`, `CaseKind`, `RiskTier`
- Produces:
  - `List<BoothCase> assignRounds(List<BoothCase> pool, Random rng)` — 길이 3, 순서 `[visible, varroa danger, varroa watch | healthy]`
  - `class TourResult { final BoothCase case_; final bool? guess; bool get correct; }`
  - `BoothSession.startTour(List<BoothCase>)`, `.rounds`, `.roundIndex`, `.currentCase`, `.recordGuess(bool?)`, `.results`, `.correctCount`, `.isLastRound`, `.reset()`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/booth/test/tour_test.dart`:

```dart
import 'dart:math';

import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/case_kind.dart';
import 'package:helpbee_booth/data/risk_tier.dart';
import 'package:helpbee_booth/data/tour.dart';

BoothCase _c(String id, CaseKind kind, RiskTier tier) => BoothCase(
  id: id,
  photo: 'photos/$id.jpg',
  kind: kind,
  disease: kind == CaseKind.healthy ? null : 'x',
  diseaseLabel: kind == CaseKind.healthy ? null : '병',
  imageWidth: 1920,
  imageHeight: 1080,
  riskScore: 0,
  tier: tier,
  beeTotal: 10,
  sickCount: kind == CaseKind.healthy ? 0 : 2,
  recommendations: const [],
  boxes: const [],
);

/// 실제 풀과 같은 구성 (visible 3 · varroa danger 3 · varroa watch 2 · healthy 2).
List<BoothCase> _pool() => [
  _c('dwv-1', CaseKind.visible, RiskTier.safe),
  _c('dwv-2', CaseKind.visible, RiskTier.safe),
  _c('chalk-1', CaseKind.visible, RiskTier.safe),
  _c('danger-100', CaseKind.varroa, RiskTier.danger),
  _c('danger-90', CaseKind.varroa, RiskTier.danger),
  _c('danger-3', CaseKind.varroa, RiskTier.danger),
  _c('watch-50', CaseKind.varroa, RiskTier.watch),
  _c('watch-2', CaseKind.varroa, RiskTier.watch),
  _c('safe-0', CaseKind.healthy, RiskTier.safe),
  _c('safe-2', CaseKind.healthy, RiskTier.safe),
];

void main() {
  test('난이도 계단대로 배정한다 — 쉬움 → 어려움 → 더 어려움', () {
    for (var seed = 0; seed < 50; seed++) {
      final r = assignRounds(_pool(), Random(seed));
      expect(r, hasLength(3));
      expect(r[0].kind, CaseKind.visible, reason: 'R1 은 눈에 보이는 병 (seed $seed)');
      expect(r[1].kind, CaseKind.varroa, reason: 'R2 는 응애 (seed $seed)');
      expect(r[1].tier, RiskTier.danger, reason: 'R2 는 위험 등급 (seed $seed)');
      expect(
        r[2].kind == CaseKind.healthy ||
            (r[2].kind == CaseKind.varroa && r[2].tier == RiskTier.watch),
        isTrue,
        reason: 'R3 는 응애 주의 또는 정상(함정) (seed $seed)',
      );
      expect(r.map((c) => c.id).toSet(), hasLength(3), reason: '같은 사진 중복 (seed $seed)');
    }
  });

  test('세션마다 조합이 달라진다', () {
    final combos = <String>{};
    for (var seed = 0; seed < 40; seed++) {
      combos.add(assignRounds(_pool(), Random(seed)).map((c) => c.id).join(','));
    }
    expect(combos.length, greaterThan(5), reason: '매번 같은 3장이면 반복 관람객이 지루하다');
  });

  test('R3 의 함정(정상)이 가끔 나온다', () {
    var healthy = 0;
    for (var seed = 0; seed < 200; seed++) {
      if (assignRounds(_pool(), Random(seed))[2].kind == CaseKind.healthy) healthy++;
    }
    expect(healthy, greaterThan(50), reason: '정상이 거의 안 나오면 함정이 성립 안 한다');
    expect(healthy, lessThan(150), reason: '정상만 나오면 응애 주의를 못 본다');
  });

  test('맞음 판정은 kind 기준이다', () {
    final visible = TourResult(case_: _c('dwv-1', CaseKind.visible, RiskTier.safe), guess: false);
    expect(visible.correct, isTrue, reason: '병든 벌통에 "문제 있음" 은 정답');

    final healthy = TourResult(case_: _c('safe-0', CaseKind.healthy, RiskTier.safe), guess: false);
    expect(healthy.correct, isFalse, reason: '건강한 벌통에 "문제 있음" 은 오답(함정)');

    final skipped = TourResult(case_: _c('safe-0', CaseKind.healthy, RiskTier.safe), guess: null);
    expect(skipped.correct, isFalse, reason: '건너뛴 라운드는 맞힌 것으로 세지 않는다');
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd apps/booth && flutter test test/tour_test.dart`
Expected: 컴파일 실패 — `Undefined name 'assignRounds'`

- [ ] **Step 3: `tour.dart` 를 만든다**

```dart
import 'dart:math';

import 'booth_case.dart';
import 'case_kind.dart';
import 'risk_tier.dart';

/// 라운드 하나의 결과 — 어떤 사진에 관람객이 뭐라고 답했는가.
class TourResult {
  const TourResult({required this.case_, required this.guess});

  final BoothCase case_;

  /// true=건강함 · false=문제 있음 · null=건너뜀
  final bool? guess;

  /// 건너뛴 라운드는 맞힌 것으로 세지 않는다 — 요약의 "N장 맞힘"이 부풀면
  /// "HelpBee 는 3장 다 찾았습니다" 대비가 약해진다.
  bool get correct => guess != null && guess == case_.isHealthy;
}

/// 풀에서 3장을 뽑아 **난이도 계단** 순서로 배정한다 (설계 §2).
///
///   R1 쉬움      눈에 보이는 병      → 대개 맞힘. 자신감이 붙는다
///   R2 어려움    응애 위험           → 대개 틀림. "어? 뭐가 문제였지"
///   R3 더 어려움  응애 주의 | 정상     → 더 미세하거나, 의심병을 찌르는 함정
///
/// 순서가 이야기다. R1 의 성공이 있어야 R2 의 실패가 대비로 살고, R3 에 뭐가
/// 나올지 모르므로 마지막까지 진지하게 본다.
///
/// 세 후보군이 서로 겹치지 않아(R2 는 danger, R3 는 watch|healthy) 중복 걱정이
/// 없다. 후보가 빈 군이 있으면 그 군은 풀 전체에서 아무거나 채운다 — 부스가
/// 데이터 사고로 멈추는 것보다는 순서가 틀리는 게 낫다.
List<BoothCase> assignRounds(List<BoothCase> pool, Random rng) {
  BoothCase pick(bool Function(BoothCase) where) {
    final candidates = pool.where(where).toList(growable: false);
    final from = candidates.isEmpty ? pool : candidates;
    return from[rng.nextInt(from.length)];
  }

  final r1 = pick((c) => c.kind == CaseKind.visible);
  final r2 = pick((c) => c.kind == CaseKind.varroa && c.tier == RiskTier.danger);
  final r3 = pick(
    (c) =>
        c.kind == CaseKind.healthy ||
        (c.kind == CaseKind.varroa && c.tier == RiskTier.watch),
  );
  return [r1, r2, r3];
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `cd apps/booth && flutter test test/tour_test.dart`
Expected: PASS (4개)

- [ ] **Step 5: 뮤테이션 — 순서를 섞어본다**

`assignRounds` 의 반환을 `[r2, r1, r3]` 로 바꿔 `'R1 은 눈에 보이는 병'` 이 실패하는지 확인하고 되돌린다.

- [ ] **Step 6: `booth_session.dart` 를 투어 상태로 바꾼다**

파일 전체를 교체:

```dart
import 'dart:math';

import 'package:flutter/foundation.dart';

import 'booth_case.dart';
import 'tour.dart';

/// 관람객 한 명의 투어 상태 (설계 §2).
///
/// 2026-09-08: 추이 그래프(seedScores/chartScores)는 제거됐다 — 요약 화면이
/// "이번 체험 3장"을 보여주므로 역할이 겹쳤다.
class BoothSession extends ChangeNotifier {
  static const int roundCount = 3;

  List<BoothCase> _rounds = const [];
  final List<TourResult> _results = [];

  List<BoothCase> get rounds => List.unmodifiable(_rounds);
  List<TourResult> get results => List.unmodifiable(_results);

  /// 지금 몇 번째 라운드인가 (0-based). 아직 답하지 않은 라운드를 가리킨다.
  int get roundIndex => _results.length;

  bool get isLastRound => roundIndex == roundCount - 1;

  BoothCase? get currentCase =>
      roundIndex < _rounds.length ? _rounds[roundIndex] : null;

  int get correctCount => _results.where((r) => r.correct).length;

  /// 풀에서 3장을 뽑아 새 투어를 시작한다.
  void startTour(List<BoothCase> pool, {Random? rng}) {
    _rounds = assignRounds(pool, rng ?? Random());
    _results.clear();
    notifyListeners();
  }

  /// 현재 라운드에 대한 관람객 추측을 기록한다.
  void recordGuess(bool? guess) {
    final c = currentCase;
    if (c == null) return;
    _results.add(TourResult(case_: c, guess: guess));
    notifyListeners();
  }

  void reset() {
    _rounds = const [];
    _results.clear();
    notifyListeners();
  }
}
```

- [ ] **Step 7: 세션 테스트를 교체한다**

`apps/booth/test/booth_session_test.dart` 전체를 교체:

```dart
import 'dart:math';

import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/booth_session.dart';
import 'package:helpbee_booth/data/case_kind.dart';
import 'package:helpbee_booth/data/risk_tier.dart';

BoothCase _c(String id, CaseKind kind, RiskTier tier) => BoothCase(
  id: id,
  photo: 'photos/$id.jpg',
  kind: kind,
  disease: null,
  diseaseLabel: null,
  imageWidth: 1920,
  imageHeight: 1080,
  riskScore: 0,
  tier: tier,
  beeTotal: 10,
  sickCount: kind == CaseKind.healthy ? 0 : 2,
  recommendations: const [],
  boxes: const [],
);

List<BoothCase> _pool() => [
  _c('dwv-1', CaseKind.visible, RiskTier.safe),
  _c('danger-100', CaseKind.varroa, RiskTier.danger),
  _c('watch-50', CaseKind.varroa, RiskTier.watch),
  _c('safe-0', CaseKind.healthy, RiskTier.safe),
];

void main() {
  test('투어를 시작하면 3라운드가 잡힌다', () {
    final s = BoothSession()..startTour(_pool(), rng: Random(1));
    expect(s.rounds, hasLength(3));
    expect(s.roundIndex, 0);
    expect(s.isLastRound, isFalse);
    expect(s.currentCase, isNotNull);
  });

  test('답할 때마다 라운드가 넘어가고 마지막에서 멈춘다', () {
    final s = BoothSession()..startTour(_pool(), rng: Random(1));
    s.recordGuess(false);
    expect(s.roundIndex, 1);
    s.recordGuess(false);
    expect(s.roundIndex, 2);
    expect(s.isLastRound, isTrue);
    s.recordGuess(false);
    expect(s.roundIndex, 3);
    expect(s.currentCase, isNull, reason: '3장이 끝나면 현재 케이스가 없다');
    // 끝난 뒤 더 눌러도 결과가 늘지 않아야 한다.
    s.recordGuess(true);
    expect(s.results, hasLength(3));
  });

  test('맞힌 수를 센다 — 건너뛴 라운드는 세지 않는다', () {
    final s = BoothSession()..startTour(_pool(), rng: Random(1));
    // R1 visible(=병듦) 에 '문제 있음' → 정답
    s.recordGuess(false);
    // R2 varroa danger(=병듦) 에 '건강함' → 오답
    s.recordGuess(true);
    // R3 건너뜀 → 오답 처리
    s.recordGuess(null);
    expect(s.correctCount, 1);
  });

  test('리셋하면 라운드와 결과가 모두 비워진다', () {
    final s = BoothSession()..startTour(_pool(), rng: Random(1));
    s.recordGuess(false);
    s.reset();
    expect(s.rounds, isEmpty);
    expect(s.results, isEmpty);
    expect(s.correctCount, 0);
  });
}
```

- [ ] **Step 8: 통과를 확인한다**

Run: `cd apps/booth && flutter test test/tour_test.dart test/booth_session_test.dart`
Expected: PASS (8개)

- [ ] **Step 9: 커밋**

```bash
git add apps/booth/lib/data/tour.dart apps/booth/lib/data/booth_session.dart apps/booth/test/tour_test.dart apps/booth/test/booth_session_test.dart
git commit -m "feat(booth): 난이도 계단 라운드 배정 + 투어 세션 상태"
```

---

## Task 4: 투어 배선 — 시작 화면, 라운드 루프, 추이 그래프 삭제

**Files:**
- Create: `apps/booth/lib/screens/tour_start_screen.dart`, `apps/booth/lib/widgets/round_indicator.dart`
- Modify: `apps/booth/lib/main.dart`
- Delete: `apps/booth/lib/screens/history_screen.dart`, `apps/booth/lib/widgets/trend_chart.dart`, `apps/booth/test/trend_chart_test.dart`
- Test: `apps/booth/test/full_flow_test.dart`

**Interfaces:**
- Consumes: `BoothSession.startTour/recordGuess/currentCase/isLastRound`, `assignRounds`
- Produces: `BoothStage { attract, intro, tourStart, guess, analyzing, report, summary, picker, outro }` — `history` 제거. `TourStartScreen({required VoidCallback onStart})`, `RoundIndicator({required int index, required int total})`

- [ ] **Step 1: `RoundIndicator` 를 만든다**

`lib/widgets/round_indicator.dart`:

```dart
import 'package:flutter/material.dart';

import '../theme/app_colors.dart';
import '../theme/app_fonts.dart';

/// `1 / 3` — 투어 진행 표시.
///
/// 자간 넓힌 안내 라벨(구 `Eyebrow`)을 대신한다. 관람객에게 필요한 정보는
/// "몇 장 중 몇 번째인가" 하나뿐이라 숫자만 남긴다.
class RoundIndicator extends StatelessWidget {
  const RoundIndicator({super.key, required this.index, required this.total});

  /// 0-based.
  final int index;
  final int total;

  @override
  Widget build(BuildContext context) => Text(
    '${index + 1} / $total',
    style: const TextStyle(
      fontFamily: kBodyFont,
      fontFamilyFallback: kBodyFallback,
      fontSize: 22,
      fontWeight: FontWeight.w700,
      color: AppColors.hintBorder,
      height: 1.0,
    ),
  );
}
```

- [ ] **Step 2: `TourStartScreen` 을 만든다**

`lib/screens/tour_start_screen.dart`:

```dart
import 'package:flutter/material.dart';

import '../theme/app_colors.dart';
import '../widgets/booth_scaffold.dart';

/// 투어 진입 안내. 5초 안에 지나가는 가벼운 화면이라 사진도 카드도 없다.
///
/// 여기서 "몇 장이나 맞히실까요?"로 **관람객에게 목표를 준다** — 목표가 없으면
/// 사진을 그냥 넘기게 되고, 요약의 "N장 맞힘"도 의미가 없어진다.
class TourStartScreen extends StatelessWidget {
  const TourStartScreen({super.key, required this.onStart});

  final VoidCallback onStart;

  @override
  Widget build(BuildContext context) {
    final t = Theme.of(context).textTheme;
    return BoothScaffold(
      child: Center(
        child: Column(
          mainAxisSize: MainAxisSize.min,
          children: [
            Text(
              '사진 3장을 진단해 봅니다',
              style: t.headlineLarge,
              textAlign: TextAlign.center,
            ),
            const SizedBox(height: 14),
            Text(
              '몇 장이나 맞히실까요?',
              style: t.bodyLarge?.copyWith(color: AppColors.textSecondary),
            ),
            const SizedBox(height: 48),
            FilledButton(
              onPressed: onStart,
              style: FilledButton.styleFrom(minimumSize: const Size(320, 76)),
              child: const Text('시작하기'),
            ),
          ],
        ),
      ),
    );
  }
}
```

- [ ] **Step 3: `main.dart` 를 배선한다**

`BoothStage` 를 교체:

```dart
enum BoothStage {
  attract,
  intro,
  tourStart,
  guess,
  analyzing,
  report,
  summary,
  picker, // 요약 뒤 보너스 경로에서만
  outro,
}
```

상태 필드를 교체 (`_picked`, `_guess` 는 보너스 경로용으로 남는다):

```dart
  final BoothSession _session = BoothSession();
  final OperatorGesture _operator = OperatorGesture();
  BoothStage _stage = BoothStage.attract;
  List<BoothCase> _cases = const [];

  /// 보너스(자유 선택) 경로에서 고른 케이스. 투어 중에는 `_session.currentCase`
  /// 가 진실의 원천이고 이 값은 null 이다.
  BoothCase? _bonusCase;
  bool? _bonusGuess;

  /// 지금 화면에 그릴 케이스 — 투어 중이면 세션이, 보너스면 `_bonusCase` 가 준다.
  BoothCase? get _activeCase => _bonusCase ?? _session.currentCase;

  /// 지금 화면에 그릴 추측.
  bool? get _activeGuess =>
      _bonusCase != null ? _bonusGuess : _session.results.lastOrNull?.guess;
```

`_resetSession()` 에서 보너스 상태도 지운다:

```dart
  void _resetSession() {
    _idle?.cancel();
    _session.reset();
    setState(() {
      _stage = BoothStage.attract;
      _bonusCase = null;
      _bonusGuess = null;
      _cases = List.of(_cases)..shuffle();
    });
  }
```

`_buildStage()` 의 `switch` 를 교체:

```dart
      case BoothStage.intro:
        return IntroScreen(onDone: () => _goTo(BoothStage.tourStart));
      case BoothStage.tourStart:
        return TourStartScreen(
          onStart: () {
            _session.startTour(_cases);
            _bonusCase = null;
            _bonusGuess = null;
            _goTo(BoothStage.guess);
          },
        );
      case BoothStage.guess:
        return GuessScreen(
          key: ValueKey(_activeCase!.id),
          case_: _activeCase!,
          roundIndex: _bonusCase != null ? null : _session.roundIndex,
          roundTotal: BoothSession.roundCount,
          onAnswer: (g) {
            if (_bonusCase != null) {
              _bonusGuess = g;
            } else {
              _session.recordGuess(g);
            }
            _goTo(BoothStage.analyzing);
          },
        );
      case BoothStage.analyzing:
        return AnalyzingScreen(
          case_: _reportCase!,
          onDone: () => _goTo(BoothStage.report),
        );
      case BoothStage.report:
        return ReportScreen(
          key: ValueKey(_reportCase!.id),
          case_: _reportCase!,
          guess: _activeGuess,
          isLastRound: _bonusCase != null || _session.currentCase == null,
          isBonus: _bonusCase != null,
          onNext: () => _goTo(
            _bonusCase != null ? BoothStage.outro : BoothStage.summary,
          ),
        );
      case BoothStage.summary:
        return SummaryScreen(
          session: _session,
          onMore: () => _goTo(BoothStage.picker),
          onFinish: () => _goTo(BoothStage.outro),
        );
      case BoothStage.picker:
        return PickerScreen(
          cases: _cases,
          onPick: (c) {
            _bonusCase = c;
            _bonusGuess = null;
            _goTo(BoothStage.guess);
          },
        );
      case BoothStage.outro:
        return OutroScreen(onRestart: _resetSession);
```

`recordGuess` 가 `roundIndex` 를 앞으로 밀어 `currentCase` 가 이미 **다음** 라운드를 가리키므로, 분석·결과 화면은 방금 답한 케이스를 봐야 한다. getter 를 추가:

```dart
  /// 분석·결과 화면이 그릴 케이스 — 방금 답한 라운드다.
  /// (`recordGuess` 후 `currentCase` 는 다음 라운드를 가리킨다.)
  BoothCase? get _reportCase =>
      _bonusCase ?? _session.results.lastOrNull?.case_;
```

`build()` 의 `Scaffold(body: _buildStage())` 를 크로스페이드로 감싼다:

```dart
            Scaffold(
              body: AnimatedSwitcher(
                duration: const Duration(milliseconds: 350),
                child: KeyedSubtree(
                  key: ValueKey(_stage),
                  child: _buildStage(),
                ),
              ),
            ),
```

import 에서 `history_screen.dart` 를 지우고 `tour_start_screen.dart`, `summary_screen.dart` 를 추가한다. `lastOrNull` 을 쓰므로 `import 'dart:collection';` 은 필요 없다 (Dart 3 코어에 있다).

- [ ] **Step 4: `SummaryScreen` 자리를 임시로 만든다**

Task 6 에서 제대로 만든다. 지금은 배선이 컴파일되도록 최소 구현만 둔다 — `lib/screens/summary_screen.dart`:

```dart
import 'package:flutter/material.dart';

import '../data/booth_session.dart';
import '../widgets/booth_scaffold.dart';

/// Task 6 에서 채운다. 지금은 배선이 돌도록 최소 형태만.
class SummaryScreen extends StatelessWidget {
  const SummaryScreen({
    super.key,
    required this.session,
    required this.onMore,
    required this.onFinish,
  });

  final BoothSession session;
  final VoidCallback onMore;
  final VoidCallback onFinish;

  @override
  Widget build(BuildContext context) => BoothScaffold(
    child: Center(
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          Text(
            '3장 중 ${session.correctCount}장 맞히셨습니다',
            style: Theme.of(context).textTheme.headlineLarge,
          ),
          const SizedBox(height: 40),
          Row(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              TextButton(onPressed: onMore, child: const Text('더 해보기')),
              const SizedBox(width: 16),
              FilledButton(
                onPressed: onFinish,
                style: FilledButton.styleFrom(minimumSize: const Size(260, 76)),
                child: const Text('QR 받기 →'),
              ),
            ],
          ),
        ],
      ),
    ),
  );
}
```

- [ ] **Step 5: 추이 그래프를 지운다**

```bash
cd apps/booth
trash lib/screens/history_screen.dart lib/widgets/trend_chart.dart test/trend_chart_test.dart
grep -rn "HistoryScreen\|TrendChart\|chartScores\|seedScores" lib/ test/
```

`grep` 이 아무것도 못 찾을 때까지 참조를 지운다 (`test/layout_overflow_test.dart` 의 `'기록'` 항목, `test/full_flow_test.dart` 의 history 관련 테스트 2개, `test/fonts_test.dart` 의 `dangerLabelStyle` import).

`fonts_test.dart` 의 "캔버스에 직접 그리는 라벨" 테스트는 `trend_chart.dart` 와 함께 사라진다 — 대신 Task 7 에서 박스 라벨 태그(같은 `TextPainter` 함정)로 되살린다.

- [ ] **Step 6: `full_flow_test.dart` 를 투어 흐름으로 고친다**

`picker` 로 가던 곳을 `tourStart` → `guess` 로 바꾼다. 인트로 이후 부분의 공통 헬퍼를 파일 상단에 추가:

```dart
/// 어트랙트 → 인트로 → 투어 시작 → 1라운드 추측 화면까지.
Future<void> _enterFirstRound(WidgetTester tester) async {
  await tester.tap(find.byType(AttractScreen));
  await tester.pump();
  await tester.pump(const Duration(seconds: 26)); // intro 자동 진행(25초)
  expect(find.byType(TourStartScreen), findsOneWidget);
  await tester.tap(find.text('시작하기'));
  await tester.pump();
  expect(find.byType(GuessScreen), findsOneWidget);
}
```

"처음부터 끝까지 한 바퀴" 테스트를 3라운드 + 요약으로 교체:

```dart
  testWidgets('투어 3라운드를 돌고 요약까지 간다', (tester) async {
    await useBoothSurface(tester);
    await tester.pumpWidget(const BoothApp());
    await _waitForCasesLoaded(tester);
    await _enterFirstRound(tester);

    for (var round = 1; round <= 3; round++) {
      expect(find.byType(GuessScreen), findsOneWidget, reason: 'R$round 추측');
      await tester.tap(find.text('문제 있음'));
      await tester.pump();
      expect(find.byType(AnalyzingScreen), findsOneWidget, reason: 'R$round 분석');
      await tester.pump(const Duration(milliseconds: 4200)); // 분석 4초
      await tester.pump(const Duration(milliseconds: 400)); // 크로스페이드
      expect(find.byType(ReportScreen), findsOneWidget, reason: 'R$round 결과');
      await tester.tap(find.text(round == 3 ? '결과 보기 →' : '다음 사진 →'));
      await tester.pump();
      await tester.pump(const Duration(milliseconds: 400));
    }

    expect(find.byType(SummaryScreen), findsOneWidget);
    await disposeAll(tester);
  });
```

무동작 리셋 테스트들의 `picker` 참조를 `tourStart` 로 바꾼다. 셔플 테스트는 `PickerScreen` 이 보너스 경로로 밀렸으므로 **삭제**하고, 대신 라운드 배정 다양성은 Task 3 의 `tour_test.dart` 가 본다.

- [ ] **Step 7: 통과를 확인한다**

Run: `cd apps/booth && flutter analyze && flutter test`
Expected: 전부 PASS. (Task 5 전이라 결과 화면은 아직 응애 전용이지만 흐름은 돈다.)

- [ ] **Step 8: 커밋**

```bash
git add -A apps/booth
git commit -m "feat(booth): 3라운드 투어 배선 + 투어 시작 화면, 추이 그래프 제거"
```

---

## Task 5: 결과 화면 — kind 3분기 + 대조 문구

**Files:**
- Modify: `apps/booth/lib/screens/report_screen.dart`, `apps/booth/lib/screens/guess_screen.dart`, `apps/booth/lib/screens/analyzing_screen.dart`, `apps/booth/lib/theme/app_colors.dart`
- Test: `apps/booth/test/report_screen_test.dart`

**Interfaces:**
- Consumes: `BoothCase.kind/diseaseLabel/sickCount/beeTotal`
- Produces: `ReportScreen({required BoothCase case_, required bool? guess, required bool isLastRound, required bool isBonus, required VoidCallback onNext})`; `GuessScreen({..., int? roundIndex, int roundTotal = 3})`; `AppColors.boxDisease`; `@visibleForTesting String verdictFor(CaseKind kind, bool? guess, bool isHealthy)`

- [ ] **Step 1: 색 토큰을 추가한다**

`lib/theme/app_colors.dart` 의 티어 색 블록 아래에 추가:

```dart
  /// 응애가 아닌 다른 병(날개불구·석고병) 의심 박스. 응애 빨강과 정상 초록
  /// 사이에서 구분되어야 하므로 주황을 쓴다.
  static const Color boxDisease = Color(0xFFE07B22);
  static const Color boxDiseaseSoft = Color(0xFFFCEADA);
```

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`apps/booth/test/report_screen_test.dart` 의 `_danger()`/`_safe()` 헬퍼에 새 필드를 채우고, 아래를 추가한다:

```dart
BoothCase _visible() => const BoothCase(
  id: 'dwv-1',
  photo: 'photos/dwv-1.jpg',
  kind: CaseKind.visible,
  disease: 'dwv',
  diseaseLabel: '날개불구 바이러스',
  imageWidth: 1920,
  imageHeight: 1080,
  riskScore: 0,
  tier: RiskTier.safe,
  beeTotal: 9,
  sickCount: 4,
  recommendations: ['응애 외 다른 질병 의심 영역이 탐지되었습니다.'],
  boxes: [BoothBox(x: 100, y: 100, w: 300, h: 200, cls: 'disease')],
);
```

```dart
  testWidgets('다른 병은 게이지도 티어 배지도 없이 병 이름을 보여준다', (tester) async {
    await useBoothSurface(tester);
    await tester.pumpWidget(
      _wrap(ReportScreen(
        case_: _visible(), guess: false, isLastRound: false,
        isBonus: false, onNext: () {},
      )),
    );

    expect(find.text('날개불구 바이러스'), findsOneWidget);
    expect(find.textContaining('4마리'), findsOneWidget);
    // 티어는 *응애* 위험도라 다른 병에는 의미가 없다. compute_risk 상 safe 가
    // 나오므로, 배지를 그리면 병든 벌통에 초록 '안전' 이 붙는다.
    expect(find.text('안전'), findsNothing, reason: '다른 병에 안전 배지가 붙으면 안 된다');
    expect(find.byType(RiskGauge), findsNothing, reason: '응애 감염률 게이지는 응애 전용');
  });

  testWidgets('응애는 게이지와 티어 배지를 그대로 보여준다', (tester) async {
    await useBoothSurface(tester);
    await tester.pumpWidget(
      _wrap(ReportScreen(
        case_: _danger(), guess: null, isLastRound: false,
        isBonus: false, onNext: () {},
      )),
    );
    expect(find.byType(RiskGauge), findsOneWidget);
    expect(find.text('위험'), findsOneWidget);
  });

  group('대조 문구', () {
    test('kind 와 정오답 조합마다 다른 문구가 나온다', () {
      expect(verdictFor(CaseKind.visible, false, false),
          contains('눈에 보이는 병'));
      expect(verdictFor(CaseKind.visible, true, false),
          contains('놓치셨네요'));
      expect(verdictFor(CaseKind.varroa, false, false),
          contains('맞히셨네요'));
      expect(verdictFor(CaseKind.varroa, true, false),
          contains('2mm'));
      expect(verdictFor(CaseKind.healthy, true, true),
          contains('정말 건강합니다'));
      expect(verdictFor(CaseKind.healthy, false, true),
          contains('함정'));
      expect(verdictFor(CaseKind.varroa, null, false), isNull,
          reason: '건너뛰면 대조 줄이 없다');
    });

    test('여섯 문구가 서로 다르다', () {
      final all = <String>{
        for (final kind in [CaseKind.visible, CaseKind.varroa, CaseKind.healthy])
          for (final g in [true, false])
            verdictFor(kind, g, kind == CaseKind.healthy)!,
      };
      expect(all, hasLength(6), reason: '문구가 겹치면 라운드마다 같은 말을 한다');
    });
  });

  testWidgets('마지막 라운드가 아니면 다음 사진, 마지막이면 결과 보기', (tester) async {
    await useBoothSurface(tester);
    var next = 0;
    await tester.pumpWidget(
      _wrap(ReportScreen(
        case_: _danger(), guess: false, isLastRound: false,
        isBonus: false, onNext: () => next++,
      )),
    );
    expect(find.text('다음 사진 →'), findsOneWidget);
    expect(find.text('기록 보기'), findsNothing, reason: '투어 중엔 옆길이 없다');
    await tester.tap(find.text('다음 사진 →'));
    expect(next, 1);

    await tester.pumpWidget(
      _wrap(ReportScreen(
        case_: _danger(), guess: false, isLastRound: true,
        isBonus: false, onNext: () {},
      )),
    );
    expect(find.text('결과 보기 →'), findsOneWidget);

    await tester.pumpWidget(
      _wrap(ReportScreen(
        case_: _danger(), guess: false, isLastRound: true,
        isBonus: true, onNext: () {},
      )),
    );
    expect(find.text('마치기 →'), findsOneWidget);
  });
```

- [ ] **Step 3: 실패를 확인한다**

Run: `cd apps/booth && flutter test test/report_screen_test.dart`
Expected: 컴파일 실패 — `No named parameter with the name 'isLastRound'`

- [ ] **Step 4: 결과 화면을 고친다**

`ReportScreen` 의 생성자·필드를 교체:

```dart
  const ReportScreen({
    super.key,
    required this.case_,
    required this.guess,
    required this.isLastRound,
    required this.isBonus,
    required this.onNext,
  });

  final BoothCase case_;

  /// 관람객의 추측. true=건강함, false=문제 있음, null=건너뜀.
  final bool? guess;

  /// 투어의 마지막 라운드인가 — 주 버튼 문구가 달라진다.
  final bool isLastRound;

  /// 요약 뒤 자유 선택으로 들어온 보너스 경로인가.
  final bool isBonus;

  final VoidCallback onNext;
```

파일 하단에 순수 함수를 추가한다 (위젯 테스트로는 6조합을 다 못 도니 떼어낸다):

```dart
/// 관람객 추측과 케이스 성격에 맞는 한 줄. 건너뛰면 null.
///
/// 라운드마다 성격이 달라서 문구도 달라야 한다 — R1 은 "보이는 걸 봤다",
/// R2 는 "못 보는 게 정상이다", R3 는 "함정이었다" 가 핵심이다. 한 문구로
/// 통일하면 3라운드가 같은 말을 세 번 하는 화면이 된다.
@visibleForTesting
String? verdictFor(CaseKind kind, bool? guess, bool isHealthy) {
  if (guess == null) return null;
  final correct = guess == isHealthy;
  return switch ((kind, correct)) {
    (CaseKind.visible, true) => '잘 보셨어요. 이건 눈에 보이는 병입니다.',
    (CaseKind.visible, false) => '놓치셨네요 — 날개와 유충 색을 다시 보세요.',
    (CaseKind.varroa, true) => '맞히셨네요! 그런데 어디가 문제인지 보이셨나요?',
    (CaseKind.varroa, false) => '못 찾는 게 정상입니다. 응애는 2mm예요.',
    (CaseKind.healthy, true) => '의심하지 않고 잘 보셨네요. 정말 건강합니다.',
    (CaseKind.healthy, false) => '함정이었습니다 — 이 벌통은 건강합니다.',
  };
}
```

`_comparison` getter 를 이걸 쓰도록 바꾼다:

```dart
  ({String mine, String verdict, bool correct})? get _comparison {
    final g = widget.guess;
    if (g == null) return null;
    final verdict = verdictFor(widget.case_.kind, g, widget.case_.isHealthy);
    if (verdict == null) return null;
    return (
      mine: g ? '건강함' : '문제 있음',
      verdict: verdict,
      correct: g == widget.case_.isHealthy,
    );
  }
```

`_ResultCard` 를 kind 로 분기한다:

```dart
class _ResultCard extends StatelessWidget {
  const _ResultCard({required this.case_});

  final BoothCase case_;

  @override
  Widget build(BuildContext context) {
    final t = Theme.of(context).textTheme;

    // 다른 병: 게이지도 티어 배지도 없다. 티어는 *응애* 감염률 개념이라
    // 다른 병에는 safe 가 나오고, 그러면 병든 벌통에 초록 '안전' 이 붙는다.
    if (case_.kind == CaseKind.visible) {
      return BoothCard(
        padding: const EdgeInsets.all(22),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          mainAxisSize: MainAxisSize.min,
          children: [
            Text(
              case_.diseaseLabel ?? '질병 의심',
              style: t.headlineMedium?.copyWith(color: AppColors.boxDisease),
            ),
            const SizedBox(height: 12),
            Text(
              '벌 ${case_.beeTotal}마리 중\n${case_.sickCount}마리 의심',
              style: t.bodyLarge?.copyWith(fontWeight: FontWeight.w600),
            ),
          ],
        ),
      );
    }

    return BoothCard(
      padding: const EdgeInsets.all(22),
      child: Row(
        children: [
          RiskGauge(
            score: case_.riskScore,
            tier: case_.tier,
            size: 190,
            stroke: 18,
          ),
          const SizedBox(width: 20),
          Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              mainAxisSize: MainAxisSize.min,
              children: [
                Align(
                  alignment: Alignment.centerLeft,
                  child: TierBadge(tier: case_.tier, scale: 0.9),
                ),
                const SizedBox(height: 14),
                Text(
                  case_.sickCount == 0
                      ? '감염 의심 개체 없음'
                      : '벌 ${case_.beeTotal}마리 중\n${case_.sickCount}마리 감염 의심',
                  style: t.bodyLarge?.copyWith(fontWeight: FontWeight.w600),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
```

범례를 3색으로 바꾼다:

```dart
                const Row(
                  mainAxisAlignment: MainAxisAlignment.start,
                  children: [
                    _Legend(color: AppColors.tierDanger, label: '응애 의심'),
                    SizedBox(width: 18),
                    _Legend(color: AppColors.boxDisease, label: '다른 병 의심'),
                    SizedBox(width: 18),
                    _Legend(color: AppColors.tierSafe, label: '정상 벌'),
                  ],
                ),
```

하단 버튼 줄을 교체 (`기록 보기`·`다른 사진 해보기`·`QR 받기` 삭제):

```dart
                Row(
                  children: [
                    const Spacer(),
                    FilledButton(
                      onPressed: widget.onNext,
                      style: FilledButton.styleFrom(
                        minimumSize: const Size(300, 76),
                      ),
                      child: Text(
                        widget.isBonus
                            ? '마치기 →'
                            : (widget.isLastRound ? '결과 보기 →' : '다음 사진 →'),
                      ),
                    ),
                  ],
                ),
```

`_ComparisonCard` 의 `correct` 색은 그대로 두되, `visible` 오답도 "혼내지 않는" 톤이라 `amberDeep` 유지.

- [ ] **Step 5: 추측·분석 화면에 라운드 표시와 4초를 넣는다**

`guess_screen.dart` 에 파라미터를 추가하고 헤드라인 우측에 표시:

```dart
  const GuessScreen({
    super.key,
    required this.case_,
    required this.onAnswer,
    this.roundIndex,
    this.roundTotal = 3,
  });

  /// 투어 라운드 (0-based). 보너스 경로면 null — 표시하지 않는다.
  final int? roundIndex;
  final int roundTotal;
```

```dart
          Row(
            crossAxisAlignment: CrossAxisAlignment.baseline,
            textBaseline: TextBaseline.alphabetic,
            children: [
              Expanded(
                child: Text('이 벌통, 건강해 보이나요?', style: t.headlineLarge),
              ),
              if (roundIndex != null)
                RoundIndicator(index: roundIndex!, total: roundTotal),
            ],
          ),
```

`analyzing_screen.dart` 의 단계 간격을 1.3초로 늘린다:

```dart
    // 1.5초는 관람객이 단계를 읽기도 전에 지나갔다 (2026-09-08 피드백).
    _timer = Timer.periodic(const Duration(milliseconds: 1300), (t) {
```

- [ ] **Step 6: 통과를 확인한다**

Run: `cd apps/booth && flutter analyze && flutter test`
Expected: 전부 PASS

- [ ] **Step 7: 뮤테이션**

`_ResultCard` 의 `if (case_.kind == CaseKind.visible)` 분기를 지워 "다른 병에 안전 배지가 붙으면 안 된다" 가 실패하는지 확인하고 되돌린다.

- [ ] **Step 8: 커밋**

```bash
git add -A apps/booth
git commit -m "feat(booth): 결과 화면 kind 3분기 + 라운드별 대조 문구 + 분석 4초"
```

---

## Task 6: 투어 요약 화면

**Files:**
- Modify: `apps/booth/lib/screens/summary_screen.dart`
- Test: `apps/booth/test/summary_screen_test.dart` (신규)

**Interfaces:**
- Consumes: `BoothSession.results/correctCount`, `TourResult.correct`, `BoothCase.kind/photo/diseaseLabel`
- Produces: `@visibleForTesting String summaryHeadline(int correct, int total)`, `@visibleForTesting String roundCaption(CaseKind kind)`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/booth/test/summary_screen_test.dart`:

```dart
import 'dart:math';

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/booth_session.dart';
import 'package:helpbee_booth/data/case_kind.dart';
import 'package:helpbee_booth/data/risk_tier.dart';
import 'package:helpbee_booth/screens/summary_screen.dart';
import 'package:helpbee_booth/theme/app_theme.dart';

import 'test_surface.dart';

BoothCase _c(String id, CaseKind kind, RiskTier tier) => BoothCase(
  id: id,
  photo: 'photos/$id.jpg',
  kind: kind,
  disease: kind == CaseKind.healthy ? null : 'x',
  diseaseLabel: kind == CaseKind.visible ? '날개불구 바이러스' : null,
  imageWidth: 1920,
  imageHeight: 1080,
  riskScore: 0,
  tier: tier,
  beeTotal: 10,
  sickCount: kind == CaseKind.healthy ? 0 : 2,
  recommendations: const [],
  boxes: const [],
);

BoothSession _session(List<bool?> guesses) {
  final s = BoothSession()
    ..startTour([
      _c('dwv-1', CaseKind.visible, RiskTier.safe),
      _c('danger-100', CaseKind.varroa, RiskTier.danger),
      _c('safe-0', CaseKind.healthy, RiskTier.safe),
    ], rng: Random(0));
  for (final g in guesses) {
    s.recordGuess(g);
  }
  return s;
}

Widget _wrap(BoothSession s) => MaterialApp(
  theme: boothTheme(),
  home: Scaffold(
    body: SummaryScreen(session: s, onMore: () {}, onFinish: () {}),
  ),
);

void main() {
  test('맞힌 수에 따라 첫 줄이 달라진다', () {
    expect(summaryHeadline(0, 3), contains('한 장도'));
    expect(summaryHeadline(1, 3), contains('1장'));
    expect(summaryHeadline(3, 3), contains('눈이 좋으시네요'));
    // 0장과 3장은 서로 달라야 한다 — 같으면 결과가 무의미해진다.
    expect(summaryHeadline(0, 3), isNot(summaryHeadline(3, 3)));
  });

  test('라운드 성격 캡션이 종류마다 다르다', () {
    expect(roundCaption(CaseKind.visible), '보이는 병');
    expect(roundCaption(CaseKind.varroa), '안 보이는 병');
    expect(roundCaption(CaseKind.healthy), '함정');
  });

  testWidgets('세 라운드 결과를 정오답과 함께 보여준다', (tester) async {
    await useBoothSurface(tester);
    // R1 정답(병든 벌통에 '문제 있음') / R2 오답 / R3 정답(건강한데 '건강함')
    await tester.pumpWidget(_wrap(_session([false, true, true])));

    expect(find.textContaining('2장'), findsWidgets);
    expect(find.text('보이는 병'), findsOneWidget);
    expect(find.text('안 보이는 병'), findsOneWidget);
    expect(find.text('함정'), findsOneWidget);
    expect(find.text('✓'), findsNWidgets(2));
    expect(find.text('✗'), findsOneWidget);
    expect(find.textContaining('HelpBee 는 3장 다 찾았습니다'), findsOneWidget);
  });

  testWidgets('건너뛴 라운드는 — 로 표시한다', (tester) async {
    await useBoothSurface(tester);
    await tester.pumpWidget(_wrap(_session([false, null, null])));
    expect(find.text('—'), findsNWidgets(2));
  });

  testWidgets('두 버튼이 각각 콜백을 부른다', (tester) async {
    await useBoothSurface(tester);
    var more = 0;
    var finish = 0;
    await tester.pumpWidget(
      MaterialApp(
        theme: boothTheme(),
        home: Scaffold(
          body: SummaryScreen(
            session: _session([false, false, false]),
            onMore: () => more++,
            onFinish: () => finish++,
          ),
        ),
      ),
    );
    await tester.tap(find.text('더 해보기'));
    await tester.tap(find.text('QR 받기 →'));
    expect(more, 1);
    expect(finish, 1);
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd apps/booth && flutter test test/summary_screen_test.dart`
Expected: 컴파일 실패 — `Undefined name 'summaryHeadline'`

- [ ] **Step 3: 요약 화면을 구현한다**

`lib/screens/summary_screen.dart` 전체를 교체:

```dart
import 'package:flutter/material.dart';

import '../data/booth_session.dart';
import '../data/case_kind.dart';
import '../data/tour.dart';
import '../theme/app_colors.dart';
import '../theme/app_radius.dart';
import '../widgets/booth_scaffold.dart';

/// 투어의 결론 화면 (설계 §4.5).
///
/// 이 화면이 이 기획의 핵심이다 — 3라운드가 만든 대비("보이는 건 맞혔고,
/// 안 보이는 건 못 맞혔다")를 한 줄로 정리해 QR 로 넘긴다.
class SummaryScreen extends StatelessWidget {
  const SummaryScreen({
    super.key,
    required this.session,
    required this.onMore,
    required this.onFinish,
  });

  final BoothSession session;
  final VoidCallback onMore;
  final VoidCallback onFinish;

  @override
  Widget build(BuildContext context) {
    final t = Theme.of(context).textTheme;
    final results = session.results;

    return BoothScaffold(
      footer: Row(
        children: [
          TextButton(onPressed: onMore, child: const Text('더 해보기')),
          const Spacer(),
          FilledButton(
            onPressed: onFinish,
            style: FilledButton.styleFrom(minimumSize: const Size(300, 76)),
            child: const Text('QR 받기 →'),
          ),
        ],
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: [
          Text(
            summaryHeadline(session.correctCount, results.length),
            style: t.headlineLarge,
            textAlign: TextAlign.center,
          ),
          const SizedBox(height: 28),
          Expanded(
            child: Row(
              children: [
                for (var i = 0; i < results.length; i++) ...[
                  if (i > 0) const SizedBox(width: 20),
                  Expanded(child: _RoundTile(result: results[i])),
                ],
              ],
            ),
          ),
          const SizedBox(height: 24),
          Text(
            '눈에 보이는 병은 찾으셨을 겁니다. 응애는 사람 눈으로는 어렵습니다.\n'
            'HelpBee 는 3장 다 찾았습니다.',
            style: t.bodyLarge?.copyWith(color: AppColors.textSecondary),
            textAlign: TextAlign.center,
          ),
        ],
      ),
    );
  }
}

/// 맞힌 수에 따른 첫 줄. 0장이라고 관람객을 탓하지 않는다 — 못 찾는 게
/// 정상이라는 게 이 부스의 메시지다.
@visibleForTesting
String summaryHeadline(int correct, int total) => switch (correct) {
  0 => '한 장도 못 찾으셨죠 — 그게 정상입니다',
  _ when correct == total => '$total장 다 맞히셨습니다. 눈이 좋으시네요',
  _ => '$total장 중 $correct장 맞히셨습니다',
};

/// 라운드가 어떤 성격이었는지 한 단어로. 관람객이 "왜 이건 맞고 저건 틀렸지"를
/// 스스로 잇게 해주는 라벨이다.
@visibleForTesting
String roundCaption(CaseKind kind) => switch (kind) {
  CaseKind.visible => '보이는 병',
  CaseKind.varroa => '안 보이는 병',
  CaseKind.healthy => '함정',
};

class _RoundTile extends StatelessWidget {
  const _RoundTile({required this.result});

  final TourResult result;

  @override
  Widget build(BuildContext context) {
    final t = Theme.of(context).textTheme;
    final skipped = result.guess == null;
    final mark = skipped ? '—' : (result.correct ? '✓' : '✗');
    final color = skipped
        ? AppColors.hintBorder
        : (result.correct ? AppColors.tierSafe : AppColors.tierDanger);

    return Column(
      crossAxisAlignment: CrossAxisAlignment.stretch,
      children: [
        Expanded(
          child: ClipRRect(
            borderRadius: BorderRadius.circular(AppRadius.photo),
            // 요약에서는 박스를 그리지 않는다 — 결과 화면에서 이미 봤고,
            // 여기서는 "어떤 사진이었는지" 기억을 되살리는 게 전부다.
            child: Image.asset(
              'assets/${result.case_.photo}',
              fit: BoxFit.cover,
            ),
          ),
        ),
        const SizedBox(height: 12),
        Row(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text(
              mark,
              style: TextStyle(
                fontSize: 26,
                fontWeight: FontWeight.w800,
                color: color,
                height: 1.0,
              ),
            ),
            const SizedBox(width: 10),
            Flexible(
              child: Text(
                roundCaption(result.case_.kind),
                style: t.bodyLarge?.copyWith(fontWeight: FontWeight.w600),
                overflow: TextOverflow.ellipsis,
              ),
            ),
          ],
        ),
      ],
    );
  }
}
```

- [ ] **Step 4: 통과를 확인한다**

Run: `cd apps/booth && flutter analyze && flutter test`
Expected: 전부 PASS

- [ ] **Step 5: 뮤테이션**

`summaryHeadline` 의 `0 =>` 분기를 지워(기본 문구로 흡수) "0장과 3장이 달라야 한다" 가 실패하는지 확인하고 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add -A apps/booth
git commit -m "feat(booth): 투어 요약 화면 — 3장 결과와 맞힌 수"
```

---

## Task 7: 바운딩 박스 — 검출기 스타일 + 감속

**Files:**
- Modify: `apps/booth/lib/widgets/bbox_overlay.dart`
- Test: `apps/booth/test/bbox_overlay_test.dart`, `apps/booth/test/fonts_test.dart`

**Interfaces:**
- Consumes: `BoothBox.cls` (`'varroa'`|`'disease'`|`'normal'`), `AppColors.boxDisease`
- Produces: `BoxStyle { Color color; double strokeWidth; double alpha; String? tag; }`, `boxStyleFor(String cls)`, `boxProgressAt(int, double)`, `totalMs(int)`, `revealOrder(List<BoothBox>)`, `boxTagStyle`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test/bbox_overlay_test.dart` 의 `boxStyleFor` 그룹을 교체하고 추가:

```dart
  group('boxStyleFor — 검출기 스타일', () {
    test('세 클래스가 색·라벨로 구분된다', () {
      final v = boxStyleFor('varroa');
      expect(v.color, AppColors.tierDanger);
      expect(v.tag, '응애');

      final d = boxStyleFor('disease');
      expect(d.color, AppColors.boxDisease);
      expect(d.tag, '질병');

      final n = boxStyleFor('normal');
      expect(n.color, AppColors.tierSafe);
      expect(n.tag, isNull, reason: '정상 벌마다 태그를 달면 화면이 글자로 덮인다');
    });

    test('모르는 cls 는 정상 벌로 취급한다', () {
      expect(boxStyleFor('nonsense').color, AppColors.tierSafe);
      expect(boxStyleFor('').color, AppColors.tierSafe);
    });

    test('선이 얇다 — 실제 검출기 출력처럼', () {
      // 굵은 선 + 둥근 모서리는 일러스트 스티커처럼 보인다 (2026-09-08 피드백).
      expect(boxStyleFor('varroa').strokeWidth, lessThanOrEqualTo(3));
      expect(boxStyleFor('normal').strokeWidth, lessThanOrEqualTo(3));
    });
  });

  group('감속', () {
    test('박스 하나가 그려지는 데 0.3초 이상 걸린다', () {
      // 240ms 는 "한 번에 다 뜬" 것처럼 보였다.
      expect(boxProgressAt(0, 200), lessThan(1.0));
      expect(boxProgressAt(0, 360), 1.0);
    });

    test('박스 사이가 충분히 벌어진다', () {
      // 첫 박스가 다 그려진 시점(360ms)에 두 번째는 아직 절반도 안 됐어야
      // "하나씩" 으로 보인다.
      expect(boxProgressAt(1, 360), lessThan(0.5));
    });

    test('박스가 많아도 총 시간이 4.5초를 넘지 않는다', () {
      // 어트랙트가 8초마다 반복하므로 그 안에 끝나야 하고, 관람객이 결과를
      // 기다리는 시간도 한계가 있다.
      for (final n in [1, 7, 14, 20, 40]) {
        expect(totalMs(n), lessThanOrEqualTo(4500), reason: '박스 $n개');
      }
      expect(totalMs(14), greaterThan(2500), reason: '너무 빨리 끝나도 안 된다');
    });

    test('시간이 지나면 모든 박스가 완성된다', () {
      for (var i = 0; i < 20; i++) {
        expect(boxProgressAt(i, totalMs(20), count: 20), 1.0);
      }
    });
  });

  group('revealOrder — 정상 → 다른 병 → 응애', () {
    test('응애가 맨 마지막에 찍힌다', () {
      const boxes = [
        BoothBox(x: 0, y: 0, w: 1, h: 1, cls: 'varroa'),
        BoothBox(x: 1, y: 0, w: 1, h: 1, cls: 'normal'),
        BoothBox(x: 2, y: 0, w: 1, h: 1, cls: 'disease'),
        BoothBox(x: 3, y: 0, w: 1, h: 1, cls: 'normal'),
      ];
      expect(
        revealOrder(boxes).map((b) => b.cls).toList(),
        ['normal', 'normal', 'disease', 'varroa'],
        reason: '빨강이 먼저 찍히면 "찾아냈다" 는 연출이 김빠진다',
      );
    });

    test('박스를 하나도 잃거나 더하지 않는다', () {
      const boxes = [
        BoothBox(x: 0, y: 0, w: 1, h: 1, cls: 'varroa'),
        BoothBox(x: 1, y: 0, w: 1, h: 1, cls: 'disease'),
        BoothBox(x: 2, y: 0, w: 1, h: 1, cls: 'nonsense'),
      ];
      expect(revealOrder(boxes).length, boxes.length);
      expect(revealOrder(const <BoothBox>[]), isEmpty);
    });
  });
```

`test/fonts_test.dart` 의 "캔버스에 직접 그리는 라벨" 테스트를 박스 태그로 되살린다 (`dangerLabelStyle` → `boxTagStyle`, import 를 `bbox_overlay.dart` 로):

```dart
  test('캔버스에 직접 그리는 박스 태그도 번들 서체를 명시한다', () {
    // TextPainter 는 위젯 트리 밖이라 테마를 상속하지 않는다 — 서체를 안 박으면
    // 웹에서 '응애'/'질병' 태그가 두부(□)가 된다.
    expect(boxTagStyle.fontFamily, kBodyFont);
    expect(boxTagStyle.fontFamilyFallback, contains(kDisplayFont));
  });
```

- [ ] **Step 2: 실패를 확인한다**

Run: `cd apps/booth && flutter test test/bbox_overlay_test.dart`
Expected: FAIL — `boxStyleFor('disease').color` 가 `tierSafe`, `tag` 없음

- [ ] **Step 3: `bbox_overlay.dart` 를 고친다**

타이밍 상수와 순수 함수를 교체:

```dart
/// 박스 하나가 그려지는 데 걸리는 시간. 240ms 는 "한 번에 다 뜬" 것처럼
/// 보였다 (2026-09-08 피드백).
const Duration _window = Duration(milliseconds: 360);

/// 박스와 박스 사이 간격 — 이 값이 "하나씩" 리듬을 만든다.
const Duration _stagger = Duration(milliseconds: 420);

/// 전체 연출이 넘으면 안 되는 상한. 박스가 14개인 케이스도 있어서, 간격을
/// 그대로 두면 6초를 넘긴다 — 관람객이 결과를 기다리다 지친다.
const double _maxTotalMs = 4500;

/// 박스 [count] 개를 다 찍는 데 걸리는 전체 시간(ms). 상한을 넘으면 간격이
/// 자동으로 줄어든다.
@visibleForTesting
double totalMs(int count) {
  final raw = _rawTotal(count);
  return raw > _maxTotalMs ? _maxTotalMs : raw;
}

double _rawTotal(int count) =>
    (count <= 1 ? 0 : (count - 1) * _stagger.inMilliseconds).toDouble() +
    _window.inMilliseconds;

/// 상한에 걸렸을 때 실제로 쓰이는 간격.
double _staggerFor(int count) {
  if (count <= 1) return _stagger.inMilliseconds.toDouble();
  final raw = _rawTotal(count);
  if (raw <= _maxTotalMs) return _stagger.inMilliseconds.toDouble();
  return (_maxTotalMs - _window.inMilliseconds) / (count - 1);
}

/// [index] 번째 박스의 진행도(0~1). 아직 차례가 오지 않았으면 0.
///
/// [count] 는 전체 박스 수 — 상한에 걸린 케이스의 간격 축소를 반영한다.
/// 생략하면 축소 없는 기본 간격을 쓴다.
@visibleForTesting
double boxProgressAt(int index, double elapsedMs, {int count = 1}) {
  final start = index * _staggerFor(count);
  final t = (elapsedMs - start) / _window.inMilliseconds;
  return Curves.easeOut.transform(t.clamp(0.0, 1.0));
}
```

`revealOrder` 에 `disease` 를 끼운다:

```dart
/// 그리는 순서 — 정상(초록) → 다른 병(주황) → 응애(빨강) 맨 마지막.
///
/// 초록으로 벌을 하나씩 훑다가 마지막에 빨강이 탁 찍혀야 "찾아냈다"는 인상이
/// 난다. 결과는 같고 **보여주는 순서만** 바꾼다.
@visibleForTesting
List<BoothBox> revealOrder(List<BoothBox> boxes) => [
  ...boxes.where((b) => b.cls != 'varroa' && b.cls != 'disease'),
  ...boxes.where((b) => b.cls == 'disease'),
  ...boxes.where((b) => b.cls == 'varroa'),
];
```

`BoxStyle` 에 태그를 추가하고 스타일을 검출기처럼 바꾼다:

```dart
/// 박스 하나의 그리기 스타일. `cls` 로만 결정된다.
@visibleForTesting
class BoxStyle {
  const BoxStyle({
    required this.color,
    required this.strokeWidth,
    required this.alpha,
    this.tag,
  });

  final Color color;
  final double strokeWidth;
  final double alpha;

  /// 박스 좌상단에 붙는 라벨. null 이면 태그를 그리지 않는다.
  final String? tag;
}

/// 실제 검출기 출력처럼 **직각·얇은 선**. 둥근 모서리에 굵은 선은
/// 일러스트 스티커처럼 보였다 (2026-09-08 피드백).
///
/// 정상 벌에는 태그를 달지 않는다 — 6~14개에 전부 '정상' 이 붙으면 화면이
/// 글자로 덮인다. 색만으로 충분하다.
@visibleForTesting
BoxStyle boxStyleFor(String cls) => switch (cls) {
  'varroa' => const BoxStyle(
    color: AppColors.tierDanger,
    strokeWidth: 3,
    alpha: 1.0,
    tag: '응애',
  ),
  'disease' => const BoxStyle(
    color: AppColors.boxDisease,
    strokeWidth: 3,
    alpha: 1.0,
    tag: '질병',
  ),
  _ => const BoxStyle(
    color: AppColors.tierSafe,
    strokeWidth: 2,
    alpha: 0.7,
  ),
};

/// 박스 라벨 태그의 글자 스타일.
///
/// ⚠️ `fontFamily` 를 반드시 명시해야 한다 — `TextPainter` 는 위젯 트리 밖이라
/// 테마의 서체를 물려받지 못하고, 웹에서 한글이 두부(□)로 그려진다.
@visibleForTesting
const TextStyle boxTagStyle = TextStyle(
  fontFamily: kBodyFont,
  fontFamilyFallback: kDisplayFallback,
  fontSize: 15,
  fontWeight: FontWeight.w700,
  height: 1.0,
  color: AppColors.surface,
);
```

`_BoxPainter.paint` 를 교체 — 직각 + 테두리가 그려지는 연출 + 태그:

```dart
  @override
  void paint(Canvas canvas, Size size) {
    final ordered = revealOrder(boxes);
    for (var i = 0; i < ordered.length; i++) {
      final b = ordered[i];
      final progress = boxProgressAt(i, elapsedMs, count: ordered.length);
      if (progress <= 0) continue;
      final style = boxStyleFor(b.cls);
      final r = fit.toScreen(Rect.fromLTWH(b.x, b.y, b.w, b.h));

      // 테두리가 좌상단에서 시계방향으로 그려진다 — 검출기가 찾아서 표시하는
      // 동작 그대로. 페이드·축소는 "그림이 밝아진" 느낌이라 쓰지 않는다.
      _drawPartialRect(canvas, r, progress, style);

      // 태그는 테두리가 다 그려진 뒤 붙는다.
      if (progress >= 1.0 && style.tag != null) {
        _drawTag(canvas, r, style, size);
      }
    }
  }

  /// 사각형 둘레를 [progress] 만큼만 그린다 (좌상단 → 시계방향).
  void _drawPartialRect(
    Canvas canvas,
    Rect r,
    double progress,
    BoxStyle style,
  ) {
    final paint = Paint()
      ..style = PaintingStyle.stroke
      ..strokeWidth = style.strokeWidth
      ..color = style.color.withValues(alpha: style.alpha);

    final path = Path()
      ..moveTo(r.left, r.top)
      ..lineTo(r.right, r.top)
      ..lineTo(r.right, r.bottom)
      ..lineTo(r.left, r.bottom)
      ..close();

    if (progress >= 1.0) {
      canvas.drawPath(path, paint);
      return;
    }
    for (final metric in path.computeMetrics()) {
      canvas.drawPath(
        metric.extractPath(0, metric.length * progress),
        paint,
      );
    }
  }

  /// 박스 좌상단 바깥에 채운 라벨. 위쪽 공간이 없으면 안쪽으로 넣는다.
  void _drawTag(Canvas canvas, Rect r, BoxStyle style, Size canvasSize) {
    // 박스가 너무 좁으면 태그가 박스보다 넓어져 지저분해진다 (석고병은
    // 박스가 소방 1칸 크기다) — 생략한다.
    if (r.width < 60) return;

    final tp = TextPainter(
      text: TextSpan(text: style.tag, style: boxTagStyle),
      textDirection: TextDirection.ltr,
    )..layout();

    const padH = 7.0;
    const padV = 4.0;
    final tagW = tp.width + padH * 2;
    final tagH = tp.height + padV * 2;
    final above = r.top - tagH >= 0;
    final origin = Offset(r.left, above ? r.top - tagH : r.top);

    canvas.drawRect(
      Rect.fromLTWH(origin.dx, origin.dy, tagW, tagH),
      Paint()..color = style.color.withValues(alpha: style.alpha),
    );
    tp.paint(canvas, origin + const Offset(padH, padV));
  }
```

파일 상단 import 에 `import '../theme/app_fonts.dart';` 를 추가한다.

- [ ] **Step 4: 통과를 확인한다**

Run: `cd apps/booth && flutter analyze && flutter test`
Expected: 전부 PASS

- [ ] **Step 5: 뮤테이션**

`_stagger` 를 `95ms` 로 되돌려 "박스 사이가 충분히 벌어진다" 가 실패하는지, `boxStyleFor('disease')` 를 `_` 분기로 흡수해 "세 클래스가 색·라벨로 구분된다" 가 실패하는지 각각 확인하고 되돌린다.

- [ ] **Step 6: 커밋**

```bash
git add -A apps/booth
git commit -m "feat(booth): 검출기 스타일 박스 — 직각·라벨 태그·테두리 그려지며 등장, 감속"
```

---

## Task 8: 디자인 걷어내기 + 전역 속도 + 실측

**Files:**
- Modify: `apps/booth/lib/widgets/booth_scaffold.dart`, `lib/widgets/brand.dart`, `lib/widgets/surfaces.dart`, `lib/widgets/risk_gauge.dart`, `lib/screens/attract_screen.dart`, `lib/screens/guess_screen.dart`, `lib/screens/outro_screen.dart`, `lib/screens/report_screen.dart`, `lib/screens/picker_screen.dart`
- Test: `apps/booth/test/layout_overflow_test.dart`, `apps/booth/test/attract_idle_test.dart`

**Interfaces:**
- Consumes: 앞 태스크 전부
- Produces: `BoothScaffold` 에서 `eyebrow` 파라미터 제거; `BoothCard` 에서 `accent` 제거; `Eyebrow` 위젯 삭제

- [ ] **Step 1: `BoothScaffold` 에서 배경 장식과 eyebrow 를 걷어낸다**

`_HoneycombPainter` 클래스 전체와 그것을 그리는 `Positioned.fill` 을 삭제한다. `dart:math` import 도 함께 지운다.
`eyebrow` 파라미터·필드와 헤더의 `if (eyebrow != null) Eyebrow(...)` 를 삭제한다.
배경 `DecoratedBox` 의 `gradient` 를 단색으로 바꾼다:

```dart
        // 그라디언트·패턴 없는 단색. 배경 장식은 화면을 "만들어진 템플릿"처럼
        // 보이게 하고, 부스에서는 사진이 주인공이라 배경이 조용할수록 낫다.
        ColoredBox(color: dark ? AppColors.boothInk : AppColors.boothBg),
```

헤더 `Row` 는 워드마크만 남는다:

```dart
          Row(children: [Wordmark(onDark: dark, size: 28)]),
```

`lib/widgets/brand.dart` 에서 `Eyebrow` 클래스를 삭제한다 (`Wordmark` 는 남긴다).

- [ ] **Step 2: 카드에서 악센트 바와 그림자를 걷어낸다**

`lib/widgets/surfaces.dart` 의 `BoothCard` 에서 `accent` 파라미터·필드와 `Stack` 분기를 삭제하고, 반경·그림자를 낮춘다:

```dart
      borderRadius: BorderRadius.circular(12),
      border: Border.all(
        color: dark ? AppColors.boothInkLine : AppColors.divider,
      ),
      // 그림자 없음 — 카드마다 그림자를 깔면 화면이 슬라이드처럼 보인다.
      // 구분은 가는 선 하나로 충분하다.
```

`PhotoFrame` 도 그림자를 뺀다 (`boxShadow` 를 지우고 `matte` 를 6 으로).

호출부에서 `accent:` 인자를 모두 지운다 (`report_screen.dart` 3곳).

- [ ] **Step 3: 게이지 글로우와 버튼 아이콘을 걷어낸다**

`lib/widgets/risk_gauge.dart` 의 `_GaugePainter.paint` 에서 blur 호가 그려지는 첫 `canvas.drawArc(...)` 블록(`maskFilter` 를 쓰는 것)을 삭제한다.

`lib/screens/guess_screen.dart` 의 `_Choice` 에서 `Icon` 과 `SizedBox(width: 14)` 를 지우고 라벨만 남긴다. `IconData icon` 파라미터도 지운다.

`lib/screens/outro_screen.dart` 의 `FilledButton.icon` 을 `FilledButton` 으로 바꾼다.

`lib/screens/attract_screen.dart` 의 안내 칩에서 `Icon(Icons.touch_app_outlined, ...)` 과 그 뒤 `SizedBox` 를 지운다. 반복 주기를 8초로 늘린다:

```dart
  /// 8초 — 박스 연출 약 3초 + 완성된 화면 5초 유지. 4초는 박스가 다 찍히자마자
  /// 다시 시작해 어수선했다.
  static const Duration replayEvery = Duration(seconds: 8);
```

- [ ] **Step 4: 결과 화면 박스 등장을 0.5초 늦춘다**

`lib/screens/report_screen.dart` 의 `BboxOverlay` 에 `startDelay` 를 넘기고, `bbox_overlay.dart` 에 파라미터를 추가한다:

```dart
    this.startDelay = Duration.zero,
```
```dart
  /// 애니메이션 시작을 늦춘다. 결과 화면은 사진을 먼저 보여준 뒤 박스가
  /// 찍혀야 "분석 결과가 나왔다"는 순서로 읽힌다.
  final Duration startDelay;
```
```dart
  @override
  void initState() {
    super.initState();
    if (!widget.animate) {
      _c.value = 1.0;
      return;
    }
    if (widget.startDelay == Duration.zero) {
      _c.forward();
    } else {
      _delay = Timer(widget.startDelay, () {
        if (mounted) _c.forward();
      });
    }
  }
```

`Timer? _delay;` 필드를 추가하고 `dispose()` 에서 `_delay?.cancel();` 한다. `import 'dart:async';` 를 추가한다.

`report_screen.dart` 호출부:

```dart
                      startDelay: const Duration(milliseconds: 500),
```

- [ ] **Step 5: picker 를 10장 5×2 로 바꾼다**

`lib/screens/picker_screen.dart` 의 `_slotAt` 과 2×2 루프를 5×2 로 바꾼다:

```dart
          Expanded(
            child: Column(
              children: [
                for (var row = 0; row < 2; row++) ...[
                  if (row > 0) const SizedBox(height: 20),
                  Expanded(
                    child: Row(
                      children: [
                        for (var col = 0; col < 5; col++) ...[
                          if (col > 0) const SizedBox(width: 20),
                          Expanded(child: _slotAt(row * 5 + col)),
                        ],
                      ],
                    ),
                  ),
                ],
              ],
            ),
          ),
```

카드가 좁아지므로 `_PhotoCardButton` 의 하단 문구를 지우고 번호 배지와 화살표만 남긴다 (10칸에 '이 사진으로 진단하기' 가 다 들어가지 않는다).

`test/picker_screen_test.dart` 의 `_cases` 를 10장으로 늘리고, `findsNWidgets(4)` 를 `findsNWidgets(10)` 으로, 마지막 카드 탭 인덱스를 `.at(9)` 으로 바꾼다.

- [ ] **Step 6: 오버플로 스윕에 새 화면을 넣는다**

`test/layout_overflow_test.dart` 의 `screens` 맵에서 `'기록'` 을 지우고 추가:

```dart
    '투어 시작': () => TourStartScreen(onStart: () {}),
    '요약': () => SummaryScreen(
      session: BoothSession()
        ..startTour([_case(), _case(), _case()], rng: Random(0))
        ..recordGuess(false)
        ..recordGuess(true)
        ..recordGuess(null),
      onMore: () {},
      onFinish: () {},
    ),
    '결과(다른 병)': () => ReportScreen(
      case_: _case(kind: CaseKind.visible),
      guess: false, isLastRound: false, isBonus: false, onNext: () {},
    ),
```

`_case()` 헬퍼에 `kind` 파라미터를 추가하고, `'사진 고르기'` 항목의 케이스 수를 10으로 늘린다. `ReportScreen` 호출부의 인자도 새 시그니처로 고친다.

- [ ] **Step 7: 서체 서브셋을 다시 굽는다**

새 문구('시작하기', '날개불구 바이러스', '함정' 등)가 늘었으므로:

```bash
cd apps/booth
# 원본 폰트 두 개를 먼저 받는다 (OFL, tool/build_fonts.py docstring 참조)
curl -sL -o /tmp/nskr.ttf "https://github.com/google/fonts/raw/main/ofl/notosanskr/NotoSansKR%5Bwght%5D.ttf"
curl -sL -o /tmp/jua.ttf "https://github.com/google/fonts/raw/main/ofl/jua/Jua-Regular.ttf"
python3 tool/build_fonts.py --noto /tmp/nskr.ttf --jua /tmp/jua.ttf
```

서브셋에 글자가 빠지면 스크립트가 실패한다. 통과하면 `flutter test test/fonts_test.dart` 로 확인한다.

- [ ] **Step 8: 전체 검증**

```bash
cd apps/booth
dart format lib test
flutter analyze          # 0 issues
flutter test             # 전부 통과
```

- [ ] **Step 9: 웹으로 전 화면을 눈으로 본다**

```bash
flutter build web --release --no-web-resources-cdn
(cd build/web && python3 -m http.server 8080)
```

브라우저에서 `localhost:8080` 을 열고 **9개 화면을 모두** 확인한다: 어트랙트 → 인트로 → 투어 시작 → (추측 → 분석 → 결과) × 3 → 요약 → 마무리, 그리고 요약에서 '더 해보기' → 자유 선택.

확인 항목: 사진이 액자를 채우는가 · 박스가 하나씩 찍히고 태그가 읽히는가 · 잘린 글자가 없는가 · 다른 병 화면에 게이지·안전 배지가 없는가.

> 사진 디코드가 끝난 뒤에야 탭이 먹는다 — 첫 화면에 사진이 보이는 걸 확인하고 클릭할 것.

- [ ] **Step 10: 아이패드 실기기로 한 바퀴 돈다**

```bash
cd apps/booth
flutter build ios --release
xcrun devicectl device install app --device 00008122-001859022633801C build/ios/iphoneos/Runner.app
```

확인 항목: 투어 한 바퀴 · 무동작 60초 리셋 · 좌상단 5탭 초기화 · QR 스캔.

- [ ] **Step 11: 문서를 갱신하고 커밋**

`docs/04-operation/booth-runbook.md` 의 화면 흐름 설명을 투어로 고치고, "관람객이 많으면 요약의 QR 받기로 바로 넘기세요" 한 줄을 운영 팁에 추가한다.

`plans/2026-09-08_부스투어-설계.md` 에 "구현 중 정정" 3건(이 계획 상단)을 반영한다.

```bash
git add -A apps/booth docs plans
git commit -m "feat(booth): AI 티 나는 장식 제거 + 전역 감속 + picker 10장"
```

- [ ] **Step 12: PR**

```bash
git push -u origin feature/booth-tour-v2
gh pr create --base develop --head feature/booth-tour-v2 \
  --title "feat(booth): 진단 투어 v2 — 3라운드 난이도 계단, 병 3종, 검출기 스타일 박스"
```

---

## 자체 점검

**스펙 커버리지**

| 설계 항목 | 태스크 |
|---|---|
| §2 흐름 (tourStart/summary, history 제거, 크로스페이드) | 4 |
| §3 데이터 (kind/disease/sickCount, 라운드 배정, 사진 10장) | 1, 2, 3 |
| §4.1 투어 시작 / §4.2 라운드 표시 / §4.3 분석 4초 | 4, 5 |
| §4.4 결과 3분기 + 대조 문구 7종 | 5 |
| §4.5 요약 | 6 |
| §4.6 picker 10장 / §4.7 삭제 | 8, 4 |
| §5 박스 (직각·태그·순서·신뢰도 없음) | 7 |
| §6 디자인 걷어내기 8항목 | 8 |
| §7 속도 7항목 | 5(분석), 7(박스), 8(어트랙트·지연·전환) |
| §8 검증 | 각 태스크 + 8의 Step 8~10 |

**정정된 설계 항목**: visible 의 recommendations(제품 코드 재사용) · visible 의 tier 배지(그리지 않음) · `isHealthy` 버그 — 계획 상단에 근거와 함께 명시했고 Task 8 Step 11 에서 설계 문서에 반영한다.

**타입 정합**: `CaseKind`(Task 2) → `assignRounds`/`TourResult`(3) → `BoothSession`(3) → `main.dart`(4) → `ReportScreen.isLastRound/isBonus/onNext`(5) → `SummaryScreen`(6) → `BoxStyle.tag`/`boxProgressAt(count:)`(7). `varroaCount` 제거는 Task 2 에서 하고 사용처(`report_screen.dart`, 테스트)는 Task 5 에서 고친다 — 그 사이 테스트가 빨간 것은 계획된 상태다.
