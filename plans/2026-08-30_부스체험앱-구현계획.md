# 부스 체험 앱 (`apps/booth`) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2026-09-15 부스에서 일반 관람객이 iPad Air 13"으로 HelpBee 진단 흐름을 백엔드 없이 체험하는 오프라인 Flutter 앱을 만든다.

**Architecture:** `apps/mobile`과 완전히 분리된 새 Flutter 앱. 실제 추론 대신 AI Hub 정답 라벨에서 `compute_risk()`로 미리 계산한 결과(`cases.json`)를 읽어 보여준다. 외부 pub 패키지를 하나도 쓰지 않아 iOS 네이티브와 `flutter build web`이 같은 코드에서 나온다.

**Tech Stack:** Flutter (SDK만) · Dart 3.10 · CustomPainter (게이지·그래프·박스) · Python 3.11 (`apps/ai` venv, 케이스 생성)

**Spec:** `plans/2026-08-30_부스체험앱-설계.md`

## Global Constraints

- **외부 pub 패키지 0개.** `pubspec.yaml`의 `dependencies`는 `flutter: sdk: flutter` 하나뿐이다. `cupertino_icons`도 넣지 않는다.
- **폰트는 TTF 직접 번들.** `google_fonts` 패키지 금지 — 런타임에 네트워크로 폰트를 받아 부스 와이파이가 끊기면 시스템 폰트로 떨어진다.
- `dart run build_runner` **금지** (Dart 3.10 build-hook 비호환 — `apps/mobile/CLAUDE.md`).
- `dart format` **레포 전체 실행 금지**. 새로 만든 `apps/booth/` 안에서만 실행한다.
- **한국어 전용.** `flutter_localizations` / `intl` 미사용, 문자열은 소스에 직접 쓴다. 날짜는 `'${d.month}월 ${d.day}일'`로 직접 포맷한다.
- **가로 고정.** 세로 방향을 `Info.plist`와 `SystemChrome` 양쪽에서 막는다.
- 파일 삭제는 `rm` 금지 → **`trash <path>`**.
- 브랜치는 **`feature/booth-demo-app`**. `develop`/`main` 직접 push 금지 (루트 `CLAUDE.md`).
- **AI Hub 원본 이미지는 절대 커밋 금지.** `apps/booth/assets/photos/`, `apps/booth/assets/varroa_closeup.jpg`는 `.gitignore`.
- 표시 문자열의 티어 한글명은 **안전 / 주의 / 위험** 고정 (`apps/mobile`의 badge 문구와 일치).

## 일정 매핑 (마감 2026-09-08)

| 태스크 | 내용 | 예상 | 완료 목표 |
|---|---|---|---|
| 1 | 케이스 데이터 파이프라인 | 0.5일 | 08-31 |
| 2 | 스캐폴드 · 테마 이식 · 로더 | 0.5일 | 08-31 |
| 3 | 좌표 변환 · 박스 오버레이 | 0.5일 | 09-01 |
| 4 | 결과 화면 | 0.5일 | 09-01 |
| 5 | 사진 선택 · 추측 · 분석중 | 0.5일 | 09-02 |
| 6 | 세션 · 어트랙트 · 인트로 · 마무리 · 운영자 제스처 | 1일 | 09-03 |
| 7 | 기록 보기 · 추세 그래프 | 0.5일 | 09-03 |
| 8 | iPad 최적화 · **실물 검증** · 런북 | 1일 | 09-05 |
| 9 | 웹 백업 배포 | 0.5일 | 09-05 |
| — | 예비 (리허설 · 사진 교체 · 사고 대응) | 2일 | 09-08 |

**Task 8이 임계 경로다.** Apple 개발자 계정이 09-05까지 준비되지 않으면 실물 설치가 막히므로,
그 시점에 웹 백업(Task 9) 단독 운영으로 전환할지 판단한다 (설계 §13 미결 1).

---

## File Structure

| 경로 | 책임 |
|---|---|
| `apps/ai/training/data/make_booth_cases.py` | 라벨 JSON → `cases.json` + 보정 사진 생성 (1회성 도구) |
| `apps/ai/app/tests/unit/test_make_booth_cases.py` | 위 스크립트의 계약 검증 |
| `apps/booth/pubspec.yaml` | 의존성 0 + 폰트/에셋 선언 |
| `apps/booth/lib/main.dart` | 앱 진입점 · 가로 고정 · 무동작 타이머 · 화면 전환 |
| `apps/booth/lib/theme/app_colors.dart` | 색 토큰 (mobile에서 복사) |
| `apps/booth/lib/theme/app_theme.dart` | ThemeData (폰트를 번들 TTF로 교체) |
| `apps/booth/lib/data/risk_tier.dart` | `RiskTier` enum + 색/한글명 (mobile에서 l10n 제거해 복사) |
| `apps/booth/lib/data/booth_case.dart` | `BoothCase` · `BoothBox` 불변 모델 + JSON 파싱 |
| `apps/booth/lib/data/case_loader.dart` | `assets/cases.json` 로드 |
| `apps/booth/lib/data/booth_session.dart` | 세션 상태 (선택 사진 · 추측 · 기록) `ChangeNotifier` |
| `apps/booth/lib/widgets/image_fit.dart` | 이미지 픽셀 ↔ 화면 좌표 변환 (순수 함수) |
| `apps/booth/lib/widgets/bbox_overlay.dart` | 사진 + 응애 박스 오버레이 + 등장 애니메이션 |
| `apps/booth/lib/widgets/risk_gauge.dart` | 반원 게이지 (mobile에서 복사) |
| `apps/booth/lib/widgets/risk_badge.dart` | 티어 배지 (mobile에서 복사) |
| `apps/booth/lib/widgets/trend_chart.dart` | 기록 꺾은선 (신규 CustomPainter) |
| `apps/booth/lib/screens/*.dart` | 화면 8개 (설계 §3의 [0]~[6] + [5-a]) |
| `apps/booth/test/*.dart` | 위젯/유닛 테스트 |

---

## Task 1: 케이스 데이터 파이프라인

라벨 JSON에서 `cases.json`과 보정 사진을 만든다. **Flutter 코드가 하나도 없어도 이 태스크만으로 완결**되며, 산출물이 이후 모든 태스크의 입력이다.

**Files:**
- Create: `apps/ai/training/data/make_booth_cases.py`
- Create: `apps/ai/app/tests/unit/test_make_booth_cases.py`
- Output (gitignored): `apps/booth/assets/cases.json`, `apps/booth/assets/photos/*.jpg`, `apps/booth/assets/varroa_closeup.jpg`

**Interfaces:**
- Consumes: `training.data.aihub_to_yolo.CLASS_MAPPING`, `app.services.risk.compute_risk`
- Produces:
  - `BoothCaseSpec(id, rel, brightness, contrast)` — dataclass
  - `BOOTH_CASES: list[BoothCaseSpec]` — 4개 진단 케이스
  - `VARROA_CLOSEUP: str` — [1] 인트로용 상대경로
  - `build_case(spec: BoothCaseSpec) -> dict` — `cases.json`의 케이스 1개
  - `cases.json` 스키마 (Task 2의 `BoothCase.fromJson`이 이 키를 그대로 읽는다):
    ```jsonc
    {"cases": [{
      "id": "danger-90", "photo": "photos/danger-90.jpg",
      "imageWidth": 1920, "imageHeight": 1080,
      "riskScore": 90, "tier": "danger",
      "beeTotal": 6, "varroaCount": 1,
      "recommendations": ["...", "..."],
      "boxes": [{"x": 224.0, "y": 413.0, "w": 814.0, "h": 664.0, "cls": "varroa"}]
    }]}
    ```

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/ai/app/tests/unit/test_make_booth_cases.py`:

```python
"""make_booth_cases 계약 검증.

Sample 데이터셋(gitignored)이 로컬에 있을 때만 실행된다. CI에서는 skip.
"""
import pathlib

import pytest

from training.data.make_booth_cases import BOOTH_CASES, SAMPLE_ROOT, build_case

pytestmark = pytest.mark.skipif(
    not (SAMPLE_ROOT / "02.라벨링데이터").exists(),
    reason="AI Hub Sample 데이터셋이 로컬에 없음",
)


def _case(case_id: str) -> dict:
    spec = next(s for s in BOOTH_CASES if s.id == case_id)
    return build_case(spec)


def test_danger_case_matches_verified_label_values():
    c = _case("danger-90")
    assert c["riskScore"] == 90
    assert c["tier"] == "danger"
    assert c["beeTotal"] == 6
    assert c["varroaCount"] == 1
    assert len(c["recommendations"]) == 5


def test_only_varroa_boxes_are_kept():
    """정상 벌 박스를 함께 그리면 화면이 덮인다 (설계 §6)."""
    c = _case("danger-90")
    assert len(c["boxes"]) == 1
    assert {b["cls"] for b in c["boxes"]} == {"varroa"}


def test_safe_case_has_no_boxes():
    c = _case("safe-0")
    assert c["riskScore"] == 0
    assert c["tier"] == "safe"
    assert c["boxes"] == []


@pytest.mark.parametrize("spec", BOOTH_CASES, ids=lambda s: s.id)
def test_every_box_is_inside_image_bounds(spec):
    """잘린 박스는 부스에서 사각형이 화면 밖으로 나가 보인다 (설계 §6 선정 기준 2)."""
    c = build_case(spec)
    for b in c["boxes"]:
        assert b["x"] >= 0 and b["y"] >= 0
        assert b["x"] + b["w"] <= c["imageWidth"]
        assert b["y"] + b["h"] <= c["imageHeight"]


def test_four_cases_cover_three_tiers():
    tiers = [build_case(s)["tier"] for s in BOOTH_CASES]
    assert len(BOOTH_CASES) == 4
    assert tiers.count("danger") == 2
    assert tiers.count("watch") == 1
    assert tiers.count("safe") == 1
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd apps/ai && ./.venv/bin/python -m pytest app/tests/unit/test_make_booth_cases.py -q
```
Expected: FAIL — `ModuleNotFoundError: No module named 'training.data.make_booth_cases'`

- [ ] **Step 3: 스크립트를 구현한다**

`apps/ai/training/data/make_booth_cases.py`:

```python
"""부스 체험 앱(apps/booth)용 고정 결과 데이터 생성기 — 1회성 도구.

모델 추론을 돌리지 않는다. AI Hub 71667 정답 라벨을 3-class로 매핑한 뒤
프로덕션 코드인 app.services.risk.compute_risk()를 그대로 호출해
risk / tier / recommendations 를 얻는다. risk.yaml 이 바뀌면 이 스크립트만
다시 돌리면 되고, 부스 앱 문구가 실제 앱과 어긋나지 않는다.

사진 선정 기준과 근거는 plans/2026-08-30_부스체험앱-설계.md §4·§6.

실행:
    cd apps/ai && ./.venv/bin/python -m training.data.make_booth_cases
"""

from __future__ import annotations

import json
import pathlib
from dataclasses import dataclass

from PIL import Image, ImageEnhance

from app.services.risk import compute_risk
from training.data.aihub_to_yolo import CLASS_MAPPING

SAMPLE_ROOT = pathlib.Path(__file__).resolve().parents[2] / "training/datasets/Sample"
BOOTH_ASSETS = pathlib.Path(__file__).resolve().parents[3] / "booth/assets"

CLASS_VARROA = 1
LONG_EDGE = 1600


@dataclass(frozen=True)
class BoothCaseSpec:
    id: str
    rel: str          # 01.원천데이터 / 02.라벨링데이터 공통 상대경로 (확장자 없음)
    brightness: float  # 표시용 보정 — 원본이 어두워 아이패드에서 안 보인다
    contrast: float


# 설계 §4. 5개 기준(응애 박스 1개 · 잘림 없음 · 면적 8~32% · 밝기≥48 · 기타질병 0)을
# 통과한 24장 중에서 티어별로 골랐다. 교체 시 기준을 반드시 재적용할 것.
BOOTH_CASES: list[BoothCaseSpec] = [
    BoothCaseSpec("danger-90", "성충/성충_응애/089/A_001_001_20230822060113_007_001_001_001", 1.25, 1.15),
    BoothCaseSpec("danger-83", "성충/성충_응애/085/A_001_001_20230822060112_002_001_001_001", 1.25, 1.15),
    BoothCaseSpec("watch-50", "성충/성충_응애/033/B_001_003_20230824081648_001_003_001_001", 1.55, 1.25),
    BoothCaseSpec("safe-0", "성충/성충_정상/076/B_001_001_20230822083847_001_003_001_000", 1.60, 1.25),
]

# [1] "응애가 뭐죠?" 화면 전용. 유충에 붙은 응애 2마리가 육안으로 보이는 유일한 계열.
# 진단 흐름에는 쓰지 않는다 (벌 1마리 = 저신뢰, 벌통 사진으로 보이지 않음).
VARROA_CLOSEUP = "유충/유충_응애/046/C_001_001_20230829142857_001_001_000_001"


def _label(rel: str) -> dict:
    return json.loads((SAMPLE_ROOT / "02.라벨링데이터" / f"{rel}.json").read_text(encoding="utf-8"))


def _source(rel: str) -> pathlib.Path:
    return SAMPLE_ROOT / "01.원천데이터" / f"{rel}.jpg"


def build_case(spec: BoothCaseSpec) -> dict:
    """라벨 JSON 1개 → cases.json 의 케이스 1개. 파일을 쓰지 않는다(테스트 가능)."""
    label = _label(spec.rel)
    width = int(label["image"]["width"])
    height = int(label["image"]["height"])

    counts = {0: 0, 1: 0, 2: 0}
    boxes: list[dict] = []
    for ann in label["annotations"]:
        cls = CLASS_MAPPING.get(ann["category_id"])
        if cls is None:
            continue
        counts[cls] += 1
        if cls != CLASS_VARROA:
            continue  # 정상 벌 박스는 버린다 — 설계 §6
        x, y, w, h = (float(v) for v in ann["bbox"])
        boxes.append({"x": x, "y": y, "w": w, "h": h, "cls": "varroa"})

    risk = compute_risk(counts)
    return {
        "id": spec.id,
        "photo": f"photos/{spec.id}.jpg",
        "imageWidth": width,
        "imageHeight": height,
        "riskScore": risk.risk_score,
        "tier": risk.tier,
        "beeTotal": risk.bee_total,
        "varroaCount": counts[CLASS_VARROA],
        "recommendations": list(risk.recommendations),
        "boxes": boxes,
    }


def _export_photo(rel: str, dest: pathlib.Path, brightness: float, contrast: float) -> None:
    """표시용 밝기·대비 보정 + 긴 변 축소. 좌표는 정규화 전 원본 픽셀 기준이므로
    cases.json 의 박스 좌표는 imageWidth/imageHeight 에 대해 그대로 유효하다."""
    img = Image.open(_source(rel)).convert("RGB")
    img = ImageEnhance.Brightness(img).enhance(brightness)
    img = ImageEnhance.Contrast(img).enhance(contrast)
    scale = LONG_EDGE / max(img.width, img.height)
    if scale < 1.0:
        img = img.resize((round(img.width * scale), round(img.height * scale)), Image.LANCZOS)
    dest.parent.mkdir(parents=True, exist_ok=True)
    img.save(dest, quality=90)


def main() -> None:
    cases = [build_case(spec) for spec in BOOTH_CASES]
    for spec in BOOTH_CASES:
        _export_photo(spec.rel, BOOTH_ASSETS / "photos" / f"{spec.id}.jpg", spec.brightness, spec.contrast)
    _export_photo(VARROA_CLOSEUP, BOOTH_ASSETS / "varroa_closeup.jpg", 1.0, 1.1)

    out = BOOTH_ASSETS / "cases.json"
    out.parent.mkdir(parents=True, exist_ok=True)
    out.write_text(json.dumps({"cases": cases}, ensure_ascii=False, indent=2), encoding="utf-8")
    for c in cases:
        print(f"{c['id']:<10} risk {c['riskScore']:>3} / {c['tier']:<6} "
              f"벌 {c['beeTotal']:>2} · 박스 {len(c['boxes'])} · 처방 {len(c['recommendations'])}")
    print(f"\n→ {out}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

```bash
cd apps/ai && ./.venv/bin/python -m pytest app/tests/unit/test_make_booth_cases.py -q
```
Expected: PASS (8 passed)

- [ ] **Step 5: 실제로 에셋을 생성하고 눈으로 확인한다**

```bash
cd apps/ai && ./.venv/bin/python -m training.data.make_booth_cases
```
Expected 출력:
```
danger-90  risk  90 / danger 벌  6 · 박스 1 · 처방 5
danger-83  risk  83 / danger 벌  7 · 박스 1 · 처방 5
watch-50   risk  50 / watch  벌 14 · 박스 1 · 처방 4
safe-0     risk   0 / safe   벌 12 · 박스 0 · 처방 3
```

생성된 `apps/booth/assets/photos/*.jpg` 4장을 **직접 열어본다.** 확인 항목:
- 사진이 어둡지 않고 벌이 알아보이는가
- 색이 과포화(핑크/보라)되지 않았는가 → 되었으면 `BOOTH_CASES`의 `brightness`/`contrast`를 낮추고 재실행
- `varroa_closeup.jpg`에서 유충에 붙은 갈색 응애 2마리가 보이는가

- [ ] **Step 6: 커밋**

```bash
git checkout -b feature/booth-demo-app
git add apps/ai/training/data/make_booth_cases.py apps/ai/app/tests/unit/test_make_booth_cases.py
git commit -m "feat(booth): AI Hub 라벨에서 부스 앱 고정 결과 생성 스크립트 추가"
```

> `apps/booth/assets/` 산출물은 커밋하지 않는다 (Task 2에서 `.gitignore` 추가).

---

## Task 2: Flutter 스캐폴드 · 테마 이식 · 케이스 로더

**Files:**
- Create: `apps/booth/` (flutter create 산출물)
- Create: `apps/booth/lib/theme/app_colors.dart`, `apps/booth/lib/theme/app_theme.dart`
- Create: `apps/booth/lib/data/risk_tier.dart`, `booth_case.dart`, `case_loader.dart`
- Create: `apps/booth/assets/fonts/Jua-Regular.ttf`, `NotoSansKR-Regular.ttf`, `NotoSansKR-Bold.ttf`
- Create: `apps/booth/test/booth_case_test.dart`
- Modify: `apps/booth/pubspec.yaml`, `.gitignore`

**Interfaces:**
- Consumes: Task 1의 `cases.json` 스키마
- Produces:
  - `enum RiskTier { safe, watch, danger, unknown }`
  - `Color riskTierColor(RiskTier)` · `String riskTierLabel(RiskTier)` → `'안전' | '주의' | '위험' | '진단 필요'`
  - `class BoothBox { final double x, y, w, h; final String cls; }`
  - `class BoothCase { final String id, photo; final int imageWidth, imageHeight, riskScore, beeTotal, varroaCount; final RiskTier tier; final List<String> recommendations; final List<BoothBox> boxes; }`
  - `BoothCase.fromJson(Map<String, dynamic>)`
  - `Future<List<BoothCase>> loadBoothCases(AssetBundle bundle)`

- [ ] **Step 1: 앱을 만들고 의존성을 0으로 만든다**

```bash
cd apps/booth 2>/dev/null || mkdir -p apps/booth
flutter create --org kr.helpbee --project-name helpbee_booth --platforms=ios,web apps/booth
```

`apps/booth/pubspec.yaml`을 아래로 **교체**한다:

```yaml
name: helpbee_booth
description: HelpBee 부스 체험 앱 — 오프라인 고정 결과 데모
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/cases.json
    - assets/varroa_closeup.jpg
    - assets/qr_mrmr.png
    - assets/photos/
  fonts:
    - family: Jua
      fonts:
        - asset: assets/fonts/Jua-Regular.ttf
    - family: NotoSansKR
      fonts:
        - asset: assets/fonts/NotoSansKR-Regular.ttf
        - asset: assets/fonts/NotoSansKR-Bold.ttf
          weight: 700
```

폰트는 Google Fonts에서 TTF를 받아 `apps/booth/assets/fonts/`에 넣는다 (둘 다 OFL 라이선스라 번들 가능). `google_fonts` 패키지를 쓰면 런타임에 네트워크로 받아 부스에서 와이파이가 끊기면 시스템 폰트로 떨어진다.

- [ ] **Step 2: 원본 이미지가 커밋되지 않게 막는다**

`apps/booth/.gitignore` 끝에 추가:

```gitignore
# AI Hub 71667 원본 — 재배포 금지(내국인 제약). 절대 커밋하지 않는다.
assets/photos/
assets/varroa_closeup.jpg
assets/cases.json
```

확인:
```bash
cd apps/booth && git check-ignore -v assets/photos/danger-90.jpg
```
Expected: `.gitignore:...:assets/photos/    assets/photos/danger-90.jpg`

- [ ] **Step 3: 실패하는 테스트를 쓴다**

`apps/booth/test/booth_case_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/risk_tier.dart';

void main() {
  test('fromJson이 티어와 박스를 읽는다', () {
    final c = BoothCase.fromJson(const {
      'id': 'danger-90',
      'photo': 'photos/danger-90.jpg',
      'imageWidth': 1920,
      'imageHeight': 1080,
      'riskScore': 90,
      'tier': 'danger',
      'beeTotal': 6,
      'varroaCount': 1,
      'recommendations': ['즉시 처치가 필요합니다.'],
      'boxes': [
        {'x': 224.0, 'y': 413.0, 'w': 814.0, 'h': 664.0, 'cls': 'varroa'},
      ],
    });

    expect(c.id, 'danger-90');
    expect(c.tier, RiskTier.danger);
    expect(c.riskScore, 90);
    expect(c.boxes, hasLength(1));
    expect(c.boxes.first.w, 814.0);
    expect(c.recommendations.first, '즉시 처치가 필요합니다.');
  });

  test('박스가 없는 정상 케이스를 읽는다', () {
    final c = BoothCase.fromJson(const {
      'id': 'safe-0',
      'photo': 'photos/safe-0.jpg',
      'imageWidth': 1920,
      'imageHeight': 1080,
      'riskScore': 0,
      'tier': 'safe',
      'beeTotal': 12,
      'varroaCount': 0,
      'recommendations': <String>[],
      'boxes': <Map<String, dynamic>>[],
    });

    expect(c.tier, RiskTier.safe);
    expect(c.boxes, isEmpty);
  });

  test('알 수 없는 tier 문자열은 unknown으로 떨어진다', () {
    expect(riskTierFromName('nonsense'), RiskTier.unknown);
  });

  test('티어 한글명은 실제 앱 배지 문구와 같다', () {
    expect(riskTierLabel(RiskTier.safe), '안전');
    expect(riskTierLabel(RiskTier.watch), '주의');
    expect(riskTierLabel(RiskTier.danger), '위험');
  });
}
```

- [ ] **Step 4: 실패를 확인한다**

```bash
cd apps/booth && flutter test test/booth_case_test.dart
```
Expected: FAIL — `Error: Couldn't resolve the package 'helpbee_booth'` 또는 `booth_case.dart` 없음

- [ ] **Step 5: `risk_tier.dart`를 만든다**

`apps/mobile/lib/core/risk/risk_tier.dart`에서 **l10n 의존을 제거**해 옮긴다.

`apps/booth/lib/data/risk_tier.dart`:

```dart
import 'package:flutter/material.dart';

import '../theme/app_colors.dart';

/// 위험도 티어. apps/mobile 의 core/risk/risk_tier.dart 를 옮겨 온 것이되,
/// 부스 앱은 한국어 전용이라 l10n 대신 문자열을 직접 쓴다.
enum RiskTier { safe, watch, danger, unknown }

Color riskTierColor(RiskTier tier) => switch (tier) {
      RiskTier.safe => AppColors.tierSafe,
      RiskTier.watch => AppColors.tierWatch,
      RiskTier.danger => AppColors.tierDanger,
      RiskTier.unknown => AppColors.tierUnknown,
    };

/// 배지 문구. apps/mobile 의 badgeSafe/Watch/Danger 와 같은 단어를 쓴다.
String riskTierLabel(RiskTier tier) => switch (tier) {
      RiskTier.safe => '안전',
      RiskTier.watch => '주의',
      RiskTier.danger => '위험',
      RiskTier.unknown => '진단 필요',
    };

/// cases.json 의 tier 문자열 → enum. 모르는 값은 unknown.
RiskTier riskTierFromName(String? name) => switch (name) {
      'safe' => RiskTier.safe,
      'watch' => RiskTier.watch,
      'danger' => RiskTier.danger,
      _ => RiskTier.unknown,
    };
```

- [ ] **Step 6: 색 토큰을 옮긴다**

`apps/mobile/lib/core/theme/app_colors.dart`를 `apps/booth/lib/theme/app_colors.dart`로 **그대로 복사**한다 (53줄, import 없음). 최소한 아래 토큰이 있어야 한다:

```dart
static const Color honeyPrimary = Color(0xFFFFD869);
static const Color honeyBrand   = Color(0xFFF9CA46);
static const Color tierSafe     = Color(0xFF2E9E5B);
static const Color tierWatch    = Color(0xFFE79D04);
static const Color tierDanger   = Color(0xFFD7443E);
static const Color tierUnknown  = Color(0xFFB9B9B9);
```

- [ ] **Step 7: `booth_case.dart`를 만든다**

`apps/booth/lib/data/booth_case.dart`:

```dart
import 'risk_tier.dart';

/// 이미지 원본 픽셀 좌표계의 박스 하나. cls 는 현재 'varroa' 뿐이다
/// (정상 벌 박스는 생성 단계에서 버린다 — 설계 §6).
class BoothBox {
  const BoothBox({
    required this.x,
    required this.y,
    required this.w,
    required this.h,
    required this.cls,
  });

  final double x;
  final double y;
  final double w;
  final double h;
  final String cls;

  factory BoothBox.fromJson(Map<String, dynamic> json) => BoothBox(
        x: (json['x'] as num).toDouble(),
        y: (json['y'] as num).toDouble(),
        w: (json['w'] as num).toDouble(),
        h: (json['h'] as num).toDouble(),
        cls: json['cls'] as String? ?? 'varroa',
      );
}

/// 사진 1장 = 체험 케이스 1개. 전부 미리 계산된 고정값이다.
class BoothCase {
  const BoothCase({
    required this.id,
    required this.photo,
    required this.imageWidth,
    required this.imageHeight,
    required this.riskScore,
    required this.tier,
    required this.beeTotal,
    required this.varroaCount,
    required this.recommendations,
    required this.boxes,
  });

  final String id;
  final String photo;
  final int imageWidth;
  final int imageHeight;
  final int riskScore;
  final RiskTier tier;
  final int beeTotal;
  final int varroaCount;
  final List<String> recommendations;
  final List<BoothBox> boxes;

  /// 관람객이 '건강함'이라고 답했을 때 맞은 것으로 칠지.
  bool get isHealthy => tier == RiskTier.safe;

  factory BoothCase.fromJson(Map<String, dynamic> json) => BoothCase(
        id: json['id'] as String,
        photo: json['photo'] as String,
        imageWidth: (json['imageWidth'] as num).toInt(),
        imageHeight: (json['imageHeight'] as num).toInt(),
        riskScore: (json['riskScore'] as num).toInt(),
        tier: riskTierFromName(json['tier'] as String?),
        beeTotal: (json['beeTotal'] as num).toInt(),
        varroaCount: (json['varroaCount'] as num).toInt(),
        recommendations: (json['recommendations'] as List<dynamic>)
            .map((e) => e as String)
            .toList(growable: false),
        boxes: (json['boxes'] as List<dynamic>)
            .map((e) => BoothBox.fromJson((e as Map).cast<String, dynamic>()))
            .toList(growable: false),
      );
}
```

- [ ] **Step 8: 테스트가 통과하는지 확인한다**

```bash
cd apps/booth && flutter test test/booth_case_test.dart
```
Expected: PASS (4 tests)

- [ ] **Step 9: `case_loader.dart`와 테마를 만든다**

`apps/booth/lib/data/case_loader.dart`:

```dart
import 'dart:convert';

import 'package:flutter/services.dart';

import 'booth_case.dart';

/// assets/cases.json 을 읽어 케이스 목록을 만든다.
/// 실패하면 그대로 던진다 — 부스 앱은 에셋이 없으면 뜰 이유가 없다.
Future<List<BoothCase>> loadBoothCases(AssetBundle bundle) async {
  final raw = await bundle.loadString('assets/cases.json');
  final decoded = jsonDecode(raw) as Map<String, dynamic>;
  return (decoded['cases'] as List<dynamic>)
      .map((e) => BoothCase.fromJson((e as Map).cast<String, dynamic>()))
      .toList(growable: false);
}
```

`apps/booth/lib/theme/app_theme.dart` — `apps/mobile/lib/core/theme/app_theme.dart`를 옮기되 `GoogleFonts.jua(...)` → `fontFamily: 'Jua'`, `GoogleFonts.notoSansKrTextTheme(...)` → `fontFamily: 'NotoSansKR'`로 바꾸고, 부스용으로 **본문 22 / 게이지 숫자 96**까지 키운다:

```dart
import 'package:flutter/material.dart';

import 'app_colors.dart';

/// 부스 전용 테마. apps/mobile 의 톤을 유지하되 서서 보는 화면이라
/// 본문 22sp, 위험도 숫자 96sp 로 키운다. 폰트는 번들 TTF (네트워크 의존 없음).
ThemeData boothTheme() {
  final base = ThemeData(
    useMaterial3: true,
    colorScheme: ColorScheme.fromSeed(seedColor: AppColors.honeyBrand),
    fontFamily: 'NotoSansKR',
  );
  return base.copyWith(
    scaffoldBackgroundColor: const Color(0xFFFFFBF0),
    textTheme: base.textTheme.copyWith(
      displayLarge: const TextStyle(fontFamily: 'Jua', fontSize: 96, height: 1.0),
      headlineLarge: const TextStyle(fontFamily: 'Jua', fontSize: 44, height: 1.2),
      headlineMedium: const TextStyle(fontFamily: 'Jua', fontSize: 32, height: 1.25),
      bodyLarge: const TextStyle(fontSize: 22, height: 1.6),
      bodyMedium: const TextStyle(fontSize: 20, height: 1.6),
    ),
  );
}
```

- [ ] **Step 10: 커밋**

```bash
cd /Users/hyeonsyu/Documents/02_Work/04_MRMR/01_HelpBee
git add apps/booth
git commit -m "feat(booth): Flutter 스캐폴드 + 테마/티어 이식 + 케이스 로더"
```

---

## Task 3: 좌표 변환과 박스 오버레이

부스에서 가장 눈에 띄는 연출이자, 틀리면 박스가 엉뚱한 곳에 그려지는 유일한 계산 로직이다. 변환을 **순수 함수로 분리**해 위젯 없이 테스트한다.

**Files:**
- Create: `apps/booth/lib/widgets/image_fit.dart`
- Create: `apps/booth/lib/widgets/bbox_overlay.dart`
- Create: `apps/booth/test/image_fit_test.dart`

**Interfaces:**
- Consumes: `BoothCase.imageWidth/imageHeight`, `BoothBox`
- Produces:
  - `class ImageFit { ImageFit({required Size imageSize, required Size boxSize}); double get scale; Offset get offset; Rect toScreen(Rect imageRect); }`
  - `class BboxOverlay extends StatefulWidget { const BboxOverlay({required this.photoAsset, required this.imageSize, required this.boxes, this.animate = true}); }`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/booth/test/image_fit_test.dart`:

```dart
import 'dart:ui';

import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/widgets/image_fit.dart';

void main() {
  group('BoxFit.contain 좌표 변환', () {
    test('세로가 남는 경우 위아래로 레터박스된다', () {
      // 1920x1080 사진을 800x600 영역에 contain → 800x450, 위아래 75씩 여백
      const fit = ImageFit(
        imageSize: Size(1920, 1080),
        boxSize: Size(800, 600),
      );
      expect(fit.scale, closeTo(0.4166667, 1e-6));
      expect(fit.offset.dx, closeTo(0, 1e-6));
      expect(fit.offset.dy, closeTo(75, 1e-6));
    });

    test('이미지 전체 사각형은 렌더 영역과 일치한다', () {
      const fit = ImageFit(imageSize: Size(1920, 1080), boxSize: Size(800, 600));
      final r = fit.toScreen(const Rect.fromLTWH(0, 0, 1920, 1080));
      expect(r.left, closeTo(0, 1e-6));
      expect(r.top, closeTo(75, 1e-6));
      expect(r.width, closeTo(800, 1e-6));
      expect(r.height, closeTo(450, 1e-6));
    });

    test('실제 박스 좌표를 화면 좌표로 옮긴다', () {
      const fit = ImageFit(imageSize: Size(1920, 1080), boxSize: Size(800, 600));
      final r = fit.toScreen(const Rect.fromLTWH(519, 71, 811, 636));
      expect(r.left, closeTo(216.25, 0.01));
      expect(r.top, closeTo(104.583, 0.01));
      expect(r.width, closeTo(337.917, 0.01));
      expect(r.height, closeTo(265.0, 0.01));
    });

    test('가로가 남는 경우 좌우로 레터박스된다', () {
      // 1000x1000 사진을 800x400 영역에 contain → 400x400, 좌우 200씩 여백
      const fit = ImageFit(imageSize: Size(1000, 1000), boxSize: Size(800, 400));
      expect(fit.scale, closeTo(0.4, 1e-6));
      expect(fit.offset.dx, closeTo(200, 1e-6));
      expect(fit.offset.dy, closeTo(0, 1e-6));
    });
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd apps/booth && flutter test test/image_fit_test.dart
```
Expected: FAIL — `image_fit.dart` 없음

- [ ] **Step 3: 변환을 구현한다**

`apps/booth/lib/widgets/image_fit.dart`:

```dart
import 'dart:math' as math;
import 'dart:ui';

/// 이미지 원본 픽셀 좌표 → 화면 좌표. `BoxFit.contain` 과 같은 규칙이다
/// (가로/세로 중 작은 배율을 쓰고 남는 쪽을 가운데 정렬).
///
/// cases.json 의 박스는 원본 픽셀 좌표이므로, 사진을 어떤 크기로 보여주든
/// 이 변환을 거쳐야 박스가 벌 위에 정확히 얹힌다.
class ImageFit {
  const ImageFit({required this.imageSize, required this.boxSize});

  final Size imageSize;
  final Size boxSize;

  double get scale => math.min(
        boxSize.width / imageSize.width,
        boxSize.height / imageSize.height,
      );

  Offset get offset => Offset(
        (boxSize.width - imageSize.width * scale) / 2,
        (boxSize.height - imageSize.height * scale) / 2,
      );

  Rect toScreen(Rect imageRect) {
    final s = scale;
    final o = offset;
    return Rect.fromLTWH(
      imageRect.left * s + o.dx,
      imageRect.top * s + o.dy,
      imageRect.width * s,
      imageRect.height * s,
    );
  }
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

```bash
cd apps/booth && flutter test test/image_fit_test.dart
```
Expected: PASS (4 tests)

- [ ] **Step 5: 오버레이 위젯을 만든다**

`apps/booth/lib/widgets/bbox_overlay.dart`:

```dart
import 'package:flutter/material.dart';

import '../data/booth_case.dart';
import '../theme/app_colors.dart';
import 'image_fit.dart';

/// 사진 + 응애 박스. 박스는 0.4초에 걸쳐 fade + 살짝 축소되며 나타난다
/// ("AI가 개체를 짚는" 연출). 정상 벌 박스는 애초에 데이터에 없다.
class BboxOverlay extends StatefulWidget {
  const BboxOverlay({
    super.key,
    required this.photoAsset,
    required this.imageSize,
    required this.boxes,
    this.animate = true,
  });

  final String photoAsset;
  final Size imageSize;
  final List<BoothBox> boxes;
  final bool animate;

  @override
  State<BboxOverlay> createState() => _BboxOverlayState();
}

class _BboxOverlayState extends State<BboxOverlay>
    with SingleTickerProviderStateMixin {
  late final AnimationController _c = AnimationController(
    vsync: this,
    duration: const Duration(milliseconds: 400),
  );

  @override
  void initState() {
    super.initState();
    if (widget.animate) {
      _c.forward();
    } else {
      _c.value = 1.0;
    }
  }

  @override
  void dispose() {
    _c.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return LayoutBuilder(
      builder: (context, constraints) {
        final fit = ImageFit(
          imageSize: widget.imageSize,
          boxSize: Size(constraints.maxWidth, constraints.maxHeight),
        );
        return Stack(
          fit: StackFit.expand,
          children: [
            Image.asset(widget.photoAsset, fit: BoxFit.contain),
            AnimatedBuilder(
              animation: _c,
              builder: (context, _) => CustomPaint(
                painter: _BoxPainter(
                  fit: fit,
                  boxes: widget.boxes,
                  progress: Curves.easeOut.transform(_c.value),
                ),
              ),
            ),
          ],
        );
      },
    );
  }
}

class _BoxPainter extends CustomPainter {
  _BoxPainter({required this.fit, required this.boxes, required this.progress});

  final ImageFit fit;
  final List<BoothBox> boxes;
  final double progress;

  @override
  void paint(Canvas canvas, Size size) {
    if (progress <= 0) return;
    final stroke = Paint()
      ..style = PaintingStyle.stroke
      ..strokeWidth = 6
      ..color = AppColors.tierDanger.withValues(alpha: progress);

    for (final b in boxes) {
      final target = fit.toScreen(Rect.fromLTWH(b.x, b.y, b.w, b.h));
      // 1.12배에서 1.0배로 조여들며 나타난다 — "짚는" 느낌
      final k = 1.0 + 0.12 * (1 - progress);
      final r = Rect.fromCenter(
        center: target.center,
        width: target.width * k,
        height: target.height * k,
      );
      canvas.drawRRect(
        RRect.fromRectAndRadius(r, const Radius.circular(8)),
        stroke,
      );
    }
  }

  @override
  bool shouldRepaint(_BoxPainter old) =>
      old.progress != progress || old.boxes != boxes;
}
```

- [ ] **Step 6: 위젯이 뜨는지 확인한다**

`apps/booth/test/bbox_overlay_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/widgets/bbox_overlay.dart';

void main() {
  testWidgets('박스가 있어도 없어도 예외 없이 렌더된다', (tester) async {
    await tester.pumpWidget(MaterialApp(
      home: SizedBox(
        width: 800,
        height: 600,
        child: BboxOverlay(
          photoAsset: 'assets/photos/danger-90.jpg',
          imageSize: const Size(1920, 1080),
          boxes: const [
            BoothBox(x: 519, y: 71, w: 811, h: 636, cls: 'varroa'),
          ],
          animate: false,
        ),
      ),
    ));
    expect(find.byType(CustomPaint), findsWidgets);

    await tester.pumpWidget(MaterialApp(
      home: SizedBox(
        width: 800,
        height: 600,
        child: BboxOverlay(
          photoAsset: 'assets/photos/safe-0.jpg',
          imageSize: const Size(1920, 1080),
          boxes: const [],
          animate: false,
        ),
      ),
    ));
    expect(tester.takeException(), isNull);
  });
}
```

```bash
cd apps/booth && flutter test
```
Expected: PASS (전체). 에셋이 없어 이미지 로드는 실패하지만 `Image.asset`은 예외를 던지지 않고 빈 박스를 그린다.

- [ ] **Step 7: 커밋**

```bash
git add apps/booth/lib/widgets apps/booth/test
git commit -m "feat(booth): 이미지 좌표 변환 + 응애 박스 오버레이"
```

---

## Task 4: 결과 화면 [5]

**Files:**
- Create: `apps/booth/lib/widgets/risk_gauge.dart`
- Create: `apps/booth/lib/screens/report_screen.dart`
- Create: `apps/booth/test/report_screen_test.dart`

**Interfaces:**
- Consumes: `BoothCase`, `BboxOverlay`, `riskTierColor`, `riskTierLabel`
- Produces:
  - `RiskGauge({required int? score, required RiskTier tier, required String caption, double size = 200, double stroke = 16})` — **`caption`은 필수다** (apps/mobile 원본 시그니처). 티어 한글명을 여기 넣고, 별도 배지를 또 그리지 않는다.
  - `class ReportScreen extends StatefulWidget { const ReportScreen({required this.case_, required this.guess, required this.onRestart, required this.onHistory, this.onFinish}); }`
    - `guess`: `bool?` — true=건강함, false=문제 있음, null=건너뜀
    - `onRestart` → **'다른 사진 해보기'** (사진 선택으로 되돌아간다. 관람객이 위험/정상을 비교해 보고 싶어 한다)
    - `onHistory` → '기록 보기'
    - `onFinish` → **nullable**. 주면 'QR 받기' 버튼이 생기고 마무리 화면으로 간다. 위젯 테스트에서는 주지 않는다.

- [ ] **Step 1: 게이지를 옮긴다**

`apps/mobile/lib/shared/widgets/risk_gauge.dart`(120줄)를 `apps/booth/lib/widgets/risk_gauge.dart`로 복사하고 import만 `../data/risk_tier.dart` / `../theme/app_colors.dart`로 고친다. 생성자는 **그대로 둔다** (`score` / `tier` / `caption` / `size` / `stroke`).

> `risk_badge.dart`는 **옮기지 않는다.** 배지와 게이지 caption이 똑같이 '위험'을 렌더해 화면에 같은 단어가 두 번 나오고, 위젯 테스트의 `findsOneWidget`도 깨진다. 티어 표기는 게이지 caption 하나로 한다.

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`apps/booth/test/report_screen_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/risk_tier.dart';
import 'package:helpbee_booth/screens/report_screen.dart';

BoothCase _danger() => const BoothCase(
      id: 'danger-90',
      photo: 'photos/danger-90.jpg',
      imageWidth: 1920,
      imageHeight: 1080,
      riskScore: 90,
      tier: RiskTier.danger,
      beeTotal: 6,
      varroaCount: 1,
      recommendations: [
        '응애 감염률이 위험 범위(10% 이상)입니다. 즉시 처치가 필요합니다.',
        '우선 alcohol wash 또는 sugar roll로 정확한 mite-per-100-bees를 측정하세요.',
      ],
      boxes: [BoothBox(x: 519, y: 71, w: 811, h: 636, cls: 'varroa')],
    );

Widget _wrap(Widget child) => MaterialApp(home: Scaffold(body: child));

void main() {
  testWidgets('위험도 점수와 티어를 보여준다', (tester) async {
    await tester.pumpWidget(_wrap(ReportScreen(
      case_: _danger(), guess: null, onRestart: () {}, onHistory: () {},
    )));
    expect(find.text('90'), findsOneWidget);
    expect(find.text('위험'), findsOneWidget);
  });

  testWidgets('처방은 첫 줄만 보이고 나머지는 접혀 있다', (tester) async {
    await tester.pumpWidget(_wrap(ReportScreen(
      case_: _danger(), guess: null, onRestart: () {}, onHistory: () {},
    )));
    expect(find.textContaining('즉시 처치가 필요합니다'), findsOneWidget);
    expect(find.textContaining('alcohol wash'), findsNothing);

    await tester.tap(find.text('처방 더 보기'));
    await tester.pumpAndSettle();
    expect(find.textContaining('alcohol wash'), findsOneWidget);
  });

  testWidgets('추측을 건너뛰면 대조 줄이 안 나온다', (tester) async {
    await tester.pumpWidget(_wrap(ReportScreen(
      case_: _danger(), guess: null, onRestart: () {}, onHistory: () {},
    )));
    expect(find.textContaining('당신'), findsNothing);
  });

  testWidgets('틀린 추측은 위로하는 문구로 대조한다', (tester) async {
    await tester.pumpWidget(_wrap(ReportScreen(
      case_: _danger(), guess: true, onRestart: () {}, onHistory: () {},
    )));
    expect(find.textContaining('전문가도 어렵습니다'), findsOneWidget);
  });
}
```

- [ ] **Step 3: 실패를 확인한다**

```bash
cd apps/booth && flutter test test/report_screen_test.dart
```
Expected: FAIL — `report_screen.dart` 없음

- [ ] **Step 4: 결과 화면을 구현한다**

좌 60% 사진+박스 / 우 40% 게이지·대조·처방의 2단. `guess`가 `null`이면 대조 줄을 그리지 않는다. 맞았으면 `'정확합니다!'`, 틀렸으면 `'눈으로는 찾기 어렵습니다 — 전문가도 어렵습니다.'`.

```dart
import 'package:flutter/material.dart';

import '../data/booth_case.dart';
import '../data/risk_tier.dart';
import '../widgets/bbox_overlay.dart';
import '../widgets/risk_badge.dart';
import '../widgets/risk_gauge.dart';

class ReportScreen extends StatefulWidget {
  const ReportScreen({
    super.key,
    required this.case_,
    required this.guess,
    required this.onRestart,
    required this.onHistory,
    this.onFinish,
  });

  final BoothCase case_;

  /// 관람객의 추측. true=건강함, false=문제 있음, null=건너뜀.
  final bool? guess;
  final VoidCallback onRestart;
  final VoidCallback onHistory;
  final VoidCallback? onFinish;

  @override
  State<ReportScreen> createState() => _ReportScreenState();
}

class _ReportScreenState extends State<ReportScreen> {
  bool _expanded = false;

  String? get _verdict {
    final g = widget.guess;
    if (g == null) return null;
    final correct = g == widget.case_.isHealthy;
    return correct
        ? '정확합니다!'
        : '눈으로는 찾기 어렵습니다 — 전문가도 어렵습니다.';
  }

  @override
  Widget build(BuildContext context) {
    final c = widget.case_;
    final recs = c.recommendations;
    return Padding(
      padding: const EdgeInsets.all(32),
      child: Row(
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: [
          Expanded(
            flex: 6,
            child: BboxOverlay(
              photoAsset: 'assets/${c.photo}',
              imageSize: Size(c.imageWidth.toDouble(), c.imageHeight.toDouble()),
              boxes: c.boxes,
            ),
          ),
          const SizedBox(width: 32),
          Expanded(
            flex: 4,
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                RiskGauge(
                  score: c.riskScore,
                  tier: c.tier,
                  caption: riskTierLabel(c.tier),
                  size: 260,
                  stroke: 22,
                ),
                if (_verdict != null) ...[
                  const SizedBox(height: 20),
                  Text(_verdict!,
                      style: Theme.of(context).textTheme.bodyLarge),
                ],
                const SizedBox(height: 24),
                if (recs.isNotEmpty)
                  Text(recs.first,
                      style: Theme.of(context).textTheme.headlineSmall),
                if (recs.length > 1 && !_expanded)
                  TextButton(
                    onPressed: () => setState(() => _expanded = true),
                    child: const Text('처방 더 보기'),
                  ),
                if (_expanded)
                  ...recs.skip(1).map((r) => Padding(
                        padding: const EdgeInsets.only(top: 12),
                        child: Text('· $r',
                            style: Theme.of(context).textTheme.bodyMedium),
                      )),
                const Spacer(),
                Row(children: [
                  TextButton(onPressed: widget.onHistory, child: const Text('기록 보기')),
                  const Spacer(),
                  if (widget.onFinish != null)
                    TextButton(onPressed: widget.onFinish, child: const Text('QR 받기')),
                  const SizedBox(width: 12),
                  FilledButton(
                    onPressed: widget.onRestart,
                    child: const Text('다른 사진 해보기'),
                  ),
                ]),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
```

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

```bash
cd apps/booth && flutter test
```
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add apps/booth/lib apps/booth/test
git commit -m "feat(booth): 결과 화면 — 게이지·박스·추측 대조·처방 접기"
```

---

## Task 5: 진단 진입 흐름 [2] [3] [4]

**Files:**
- Create: `apps/booth/lib/screens/picker_screen.dart`, `guess_screen.dart`, `analyzing_screen.dart`
- Create: `apps/booth/test/guess_screen_test.dart`, `analyzing_screen_test.dart`

**Interfaces:**
- Produces:
  - `PickerScreen({required List<BoothCase> cases, required ValueChanged<BoothCase> onPick})`
  - `GuessScreen({required BoothCase case_, required ValueChanged<bool?> onAnswer})` — 건너뛰기는 `null`
  - `AnalyzingScreen({required BoothCase case_, required VoidCallback onDone})` — 1.5초 뒤 `onDone`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`apps/booth/test/guess_screen_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/risk_tier.dart';
import 'package:helpbee_booth/screens/guess_screen.dart';

const _c = BoothCase(
  id: 'watch-50', photo: 'photos/watch-50.jpg',
  imageWidth: 1920, imageHeight: 1080,
  riskScore: 50, tier: RiskTier.watch, beeTotal: 14, varroaCount: 1,
  recommendations: ['처치를 검토하세요.'],
  boxes: [BoothBox(x: 10, y: 10, w: 100, h: 100, cls: 'varroa')],
);

void main() {
  testWidgets('세 버튼이 각각 true/false/null을 돌려준다', (tester) async {
    final answers = <Object?>[];
    Widget app() => MaterialApp(
          home: Scaffold(
            body: GuessScreen(case_: _c, onAnswer: answers.add),
          ),
        );

    await tester.pumpWidget(app());
    await tester.tap(find.text('건강함'));
    await tester.pumpWidget(app());
    await tester.tap(find.text('문제 있음'));
    await tester.pumpWidget(app());
    await tester.tap(find.text('바로 결과 보기'));

    expect(answers, [true, false, null]);
  });

  testWidgets('추측 화면에는 박스를 그리지 않는다', (tester) async {
    await tester.pumpWidget(MaterialApp(
      home: Scaffold(body: GuessScreen(case_: _c, onAnswer: (_) {})),
    ));
    // 정답(박스)을 미리 보여주면 추측의 의미가 없다.
    expect(find.text('이 벌통, 건강해 보이나요?'), findsOneWidget);
  });
}
```

`apps/booth/test/analyzing_screen_test.dart`:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/risk_tier.dart';
import 'package:helpbee_booth/screens/analyzing_screen.dart';

const _c = BoothCase(
  id: 'danger-90', photo: 'photos/danger-90.jpg',
  imageWidth: 1920, imageHeight: 1080,
  riskScore: 90, tier: RiskTier.danger, beeTotal: 6, varroaCount: 1,
  recommendations: ['즉시 처치가 필요합니다.'], boxes: [],
);

void main() {
  testWidgets('탐지 마리 수를 보여주고 1.5초 뒤 onDone을 부른다', (tester) async {
    var done = false;
    await tester.pumpWidget(MaterialApp(
      home: Scaffold(body: AnalyzingScreen(case_: _c, onDone: () => done = true)),
    ));

    // 첫 프레임은 '사진을 읽는 중'이다. 두 번째 단계에서 마리 수가 나온다.
    expect(find.text('사진을 읽는 중'), findsOneWidget);
    expect(done, isFalse);

    await tester.pump(const Duration(milliseconds: 600));
    expect(find.textContaining('6마리'), findsOneWidget);
    expect(done, isFalse);

    await tester.pump(const Duration(milliseconds: 1100));
    expect(done, isTrue);
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd apps/booth && flutter test test/guess_screen_test.dart test/analyzing_screen_test.dart
```
Expected: FAIL — 세 화면 파일 없음

- [ ] **Step 3: 세 화면을 구현한다**

`analyzing_screen.dart`의 핵심 — 1.5초 타이머와 단계 문구:

```dart
import 'dart:async';

import 'package:flutter/material.dart';

import '../data/booth_case.dart';

/// 실제 추론은 없다. 곧바로 결과를 띄우면 관람객이 "분석한 게 맞나?" 하므로
/// 1.5초 동안 파이프라인 단계를 보여준다. 문구는 실제 동작 순서를 그대로 쓴다.
class AnalyzingScreen extends StatefulWidget {
  const AnalyzingScreen({super.key, required this.case_, required this.onDone});

  final BoothCase case_;
  final VoidCallback onDone;

  @override
  State<AnalyzingScreen> createState() => _AnalyzingScreenState();
}

class _AnalyzingScreenState extends State<AnalyzingScreen> {
  Timer? _timer;
  int _step = 0;

  @override
  void initState() {
    super.initState();
    _timer = Timer.periodic(const Duration(milliseconds: 500), (t) {
      if (!mounted) return;
      setState(() => _step = t.tick);
      if (t.tick >= 3) {
        t.cancel();
        widget.onDone();
      }
    });
  }

  @override
  void dispose() {
    _timer?.cancel();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    final steps = [
      '사진을 읽는 중',
      '벌 ${widget.case_.beeTotal}마리 탐지',
      '감염 개체 판별 중',
    ];
    return Center(
      child: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          const CircularProgressIndicator(),
          const SizedBox(height: 32),
          Text(steps[_step.clamp(0, steps.length - 1)],
              style: Theme.of(context).textTheme.headlineMedium),
        ],
      ),
    );
  }
}
```

`picker_screen.dart` — 4장을 2×2 카드로. **카드에 티어를 절대 표기하지 않는다** (정답 노출 금지):

```dart
import 'package:flutter/material.dart';

import '../data/booth_case.dart';

class PickerScreen extends StatelessWidget {
  const PickerScreen({super.key, required this.cases, required this.onPick});

  final List<BoothCase> cases;
  final ValueChanged<BoothCase> onPick;

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(40),
      child: Column(
        children: [
          Text('벌통 사진을 하나 골라보세요',
              style: Theme.of(context).textTheme.headlineLarge),
          const SizedBox(height: 32),
          Expanded(
            child: GridView.count(
              crossAxisCount: 2,
              mainAxisSpacing: 24,
              crossAxisSpacing: 24,
              childAspectRatio: 16 / 9,
              children: [
                for (final c in cases)
                  // 티어·점수를 쓰지 않는다. 관람객이 답을 미리 알면 추측이 무의미해진다.
                  InkWell(
                    onTap: () => onPick(c),
                    borderRadius: BorderRadius.circular(20),
                    child: ClipRRect(
                      borderRadius: BorderRadius.circular(20),
                      child: Image.asset('assets/${c.photo}', fit: BoxFit.cover),
                    ),
                  ),
              ],
            ),
          ),
        ],
      ),
    );
  }
}
```

`guess_screen.dart` — 사진을 **박스 없이** 크게 보여주고 버튼 3개:

```dart
import 'package:flutter/material.dart';

import '../data/booth_case.dart';

/// 관람객이 눈으로 먼저 판단하게 하는 화면. 여기서 박스를 보여주면
/// 정답을 미리 알려주는 셈이라 사진만 그린다.
class GuessScreen extends StatelessWidget {
  const GuessScreen({super.key, required this.case_, required this.onAnswer});

  final BoothCase case_;

  /// true=건강함 · false=문제 있음 · null=건너뜀
  final ValueChanged<bool?> onAnswer;

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(40),
      child: Column(
        children: [
          Text('이 벌통, 건강해 보이나요?',
              style: Theme.of(context).textTheme.headlineLarge),
          const SizedBox(height: 24),
          Expanded(child: Image.asset('assets/${case_.photo}', fit: BoxFit.contain)),
          const SizedBox(height: 24),
          Row(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              _Big(label: '건강함', onTap: () => onAnswer(true)),
              const SizedBox(width: 24),
              _Big(label: '문제 있음', onTap: () => onAnswer(false)),
              const SizedBox(width: 24),
              TextButton(
                onPressed: () => onAnswer(null),
                child: const Text('바로 결과 보기'),
              ),
            ],
          ),
        ],
      ),
    );
  }
}

class _Big extends StatelessWidget {
  const _Big({required this.label, required this.onTap});

  final String label;
  final VoidCallback onTap;

  @override
  Widget build(BuildContext context) => FilledButton(
        onPressed: onTap,
        style: FilledButton.styleFrom(
          minimumSize: const Size(220, 72), // 서서 누르는 버튼
        ),
        child: Text(label, style: const TextStyle(fontSize: 26)),
      );
}
```

- [ ] **Step 4: 테스트가 통과하는지 확인한다**

```bash
cd apps/booth && flutter test
```
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add apps/booth/lib/screens apps/booth/test
git commit -m "feat(booth): 사진 선택·추측·분석중 화면"
```

---

## Task 6: 세션 · 어트랙트 · 인트로 · 마무리 · 무동작 타이머

앱을 처음으로 **끝까지 돌려볼 수 있게** 만드는 태스크다.

**Files:**
- Create: `apps/booth/lib/data/booth_session.dart`
- Create: `apps/booth/lib/screens/attract_screen.dart`, `intro_screen.dart`, `outro_screen.dart`
- Create: `apps/booth/assets/qr_mrmr.png`
- Modify: `apps/booth/lib/main.dart`
- Create: `apps/booth/test/booth_session_test.dart`

**Interfaces:**
- Consumes: `PickerScreen` · `GuessScreen` · `AnalyzingScreen` (Task 5), `ReportScreen` (Task 4), `HistoryScreen` (Task 7)
- Produces:
  - `class BoothSession extends ChangeNotifier { List<BoothCase> get history; void record(BoothCase c); void reset(); List<int> get chartScores; static const List<int> seedScores; }`
  - `chartScores` = 예시 4점 `[12, 28, 21, 45]` + 이번 세션 진단 점수들
  - `AttractScreen({required BoothCase case_, required VoidCallback onStart})`
  - `IntroScreen({required VoidCallback onDone})` — 3초 뒤 자동 `onDone`
  - `OutroScreen({required VoidCallback onRestart})` — 15초 뒤 자동 `onRestart`
  - `enum BoothStage { attract, intro, picker, guess, analyzing, report, history, outro }`

- [ ] **Step 1: QR 이미지를 만든다**

`https://www.mrmr.kr/` QR을 512×512 PNG로 만들어 `apps/booth/assets/qr_mrmr.png`에 넣는다. Python으로:

```bash
cd apps/ai && ./.venv/bin/pip install qrcode[pil] && ./.venv/bin/python -c "
import qrcode
qrcode.make('https://www.mrmr.kr/').resize((512,512)).save('../booth/assets/qr_mrmr.png')
print('ok')
"
```

**아이패드에서 실제로 스캔되는지 반드시 확인한다.** (부스 당일 이게 안 되면 마무리 화면이 무의미해진다.)

- [ ] **Step 2: 실패하는 테스트를 쓴다**

`apps/booth/test/booth_session_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/data/booth_case.dart';
import 'package:helpbee_booth/data/booth_session.dart';
import 'package:helpbee_booth/data/risk_tier.dart';

const _danger = BoothCase(
  id: 'danger-90', photo: 'p', imageWidth: 1920, imageHeight: 1080,
  riskScore: 90, tier: RiskTier.danger, beeTotal: 6, varroaCount: 1,
  recommendations: [], boxes: [],
);

void main() {
  test('예시 4점 위에 이번 세션 진단이 얹힌다', () {
    final s = BoothSession();
    expect(s.chartScores, BoothSession.seedScores);

    s.record(_danger);
    expect(s.chartScores, [...BoothSession.seedScores, 90]);
  });

  test('초기화하면 관람객 기록만 지워지고 예시 4점은 남는다', () {
    final s = BoothSession()..record(_danger);
    s.reset();
    expect(s.history, isEmpty);
    expect(s.chartScores, BoothSession.seedScores);
  });

  test('기록이 바뀌면 리스너에게 알린다', () {
    final s = BoothSession();
    var notified = 0;
    s.addListener(() => notified++);
    s.record(_danger);
    s.reset();
    expect(notified, 2);
  });
}
```

- [ ] **Step 3: 실패를 확인한다**

```bash
cd apps/booth && flutter test test/booth_session_test.dart
```
Expected: FAIL — `booth_session.dart` 없음

- [ ] **Step 4: 세션을 구현한다**

```dart
import 'package:flutter/foundation.dart';

import 'booth_case.dart';

/// 관람객 한 명의 체험 상태. 앱이 초기화되면 history 만 비우고
/// 예시 4점은 그대로 둔다 (설계 §3 [5-a]).
class BoothSession extends ChangeNotifier {
  /// 그래프가 1점짜리로 밋밋해지지 않도록 미리 심어두는 과거 4주치.
  /// 실제 데이터가 아니므로 화면에 "예시 기록"이라고 표기한다.
  static const List<int> seedScores = [12, 28, 21, 45];

  final List<BoothCase> _history = [];

  List<BoothCase> get history => List.unmodifiable(_history);

  List<int> get chartScores =>
      [...seedScores, ..._history.map((c) => c.riskScore)];

  void record(BoothCase c) {
    _history.add(c);
    notifyListeners();
  }

  void reset() {
    _history.clear();
    notifyListeners();
  }
}
```

- [ ] **Step 5: `main.dart`에 흐름과 무동작 타이머를 붙인다**

```dart
import 'dart:async';

import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

import 'data/booth_case.dart';
import 'data/booth_session.dart';
import 'data/case_loader.dart';
import 'theme/app_theme.dart';

Future<void> main() async {
  WidgetsFlutterBinding.ensureInitialized();
  // 부스 거치대는 가로다. 세로로 돌아가면 레이아웃이 깨진다.
  await SystemChrome.setPreferredOrientations([
    DeviceOrientation.landscapeLeft,
    DeviceOrientation.landscapeRight,
  ]);
  runApp(const BoothApp());
}

enum BoothStage { attract, intro, picker, guess, analyzing, report, history, outro }

class BoothApp extends StatefulWidget {
  const BoothApp({super.key});

  @override
  State<BoothApp> createState() => _BoothAppState();
}

class _BoothAppState extends State<BoothApp> {
  final BoothSession _session = BoothSession();
  BoothStage _stage = BoothStage.attract;
  List<BoothCase> _cases = const [];
  BoothCase? _picked;
  bool? _guess;
  Timer? _idle;

  static const Duration _idleTimeout = Duration(seconds: 60);

  @override
  void initState() {
    super.initState();
    loadBoothCases(rootBundle).then((cs) => setState(() => _cases = cs));
  }

  @override
  void dispose() {
    _idle?.cancel();
    _session.dispose();
    super.dispose();
  }

  /// 관람객이 그냥 가버려도 다음 사람에게 깨끗한 첫 화면이 보이게 한다.
  void _touched() {
    _idle?.cancel();
    if (_stage == BoothStage.attract) return;
    _idle = Timer(_idleTimeout, _resetSession);
  }

  void _resetSession() {
    _idle?.cancel();
    _session.reset();
    setState(() {
      _stage = BoothStage.attract;
      _picked = null;
      _guess = null;
    });
  }

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      theme: boothTheme(),
      home: Listener(
        behavior: HitTestBehavior.translucent,
        onPointerDown: (_) => _touched(),
        child: Scaffold(body: _buildStage()),
      ),
    );
  }

  Widget _buildStage() {
    if (_cases.isEmpty) return const Center(child: CircularProgressIndicator());
    switch (_stage) {
      case BoothStage.attract:
        return AttractScreen(
          case_: _cases.firstWhere((c) => c.id == 'danger-90',
              orElse: () => _cases.first),
          onStart: () => setState(() => _stage = BoothStage.intro),
        );
      case BoothStage.intro:
        return IntroScreen(
            onDone: () => setState(() => _stage = BoothStage.picker));
      case BoothStage.picker:
        return PickerScreen(
          cases: _cases,
          onPick: (c) => setState(() {
            _picked = c;
            _guess = null;
            _stage = BoothStage.guess;
          }),
        );
      case BoothStage.guess:
        return GuessScreen(
          case_: _picked!,
          onAnswer: (g) => setState(() {
            _guess = g;
            _stage = BoothStage.analyzing;
          }),
        );
      case BoothStage.analyzing:
        return AnalyzingScreen(
          case_: _picked!,
          onDone: () {
            _session.record(_picked!);
            setState(() => _stage = BoothStage.report);
          },
        );
      case BoothStage.report:
        return ReportScreen(
          case_: _picked!,
          guess: _guess,
          onRestart: () => setState(() => _stage = BoothStage.picker),
          onHistory: () => setState(() => _stage = BoothStage.history),
          onFinish: () => setState(() => _stage = BoothStage.outro),
        );
      case BoothStage.history:
        return HistoryScreen(
          session: _session,
          onBack: () => setState(() => _stage = BoothStage.report),
        );
      case BoothStage.outro:
        return OutroScreen(onRestart: _resetSession);
    }
  }
}
```

- [ ] **Step 6: 화면들을 `_buildStage()`에 연결하고 손으로 한 바퀴 돌린다**

`attract_screen.dart`는 `danger-90` 케이스로 `BboxOverlay`와 게이지를 4초 주기로 자동 반복 재생한다.
`intro_screen.dart`는 `assets/varroa_closeup.jpg` + `"꿀벌에 붙은 이 진드기가 벌통을 무너뜨립니다"` + 3초 자동 진행.
`outro_screen.dart`는 `assets/qr_mrmr.png` 크게 + `[다시 하기]` + 15초 후 `_resetSession()`.

```bash
cd apps/booth && flutter run -d "iPad Air 13-inch (M3)"
```

손으로 확인:
- 어트랙트 → 터치 → 인트로 → 3초 자동 → 사진 4장
- 사진 탭 → 추측 → 분석중 1.5초 → 결과 (박스 등장)
- 다시 하기 → 사진 4장으로
- 마무리 QR에서 15초 대기 → 어트랙트 복귀
- 아무 화면에서 60초 방치 → 어트랙트 복귀

- [ ] **Step 7: 운영자 강제 초기화 제스처를 넣는다** (설계 §9)

부스 중 앱이 이상한 상태로 굳으면 운영자가 즉시 되돌릴 수단이 필요하다. 관람객이
우연히 누르지 않도록 **좌상단 60×60 영역을 3초 안에 5번** 탭해야 발동한다.

`main.dart`의 `Scaffold(body: ...)`를 `Stack`으로 감싸고 좌상단에 투명 감지 영역을 얹는다:

```dart
  final List<DateTime> _taps = [];

  /// 좌상단 5연타 → 강제 초기화. 관람객 오작동 방지를 위해 3초 창을 둔다.
  void _operatorTap() {
    final now = DateTime.now();
    _taps.add(now);
    _taps.removeWhere((t) => now.difference(t) > const Duration(seconds: 3));
    if (_taps.length >= 5) {
      _taps.clear();
      _resetSession();
    }
  }
```

```dart
        child: Stack(children: [
          Scaffold(body: _buildStage()),
          Positioned(
            left: 0,
            top: 0,
            child: GestureDetector(
              behavior: HitTestBehavior.opaque,
              onTap: _operatorTap,
              child: const SizedBox(width: 60, height: 60),
            ),
          ),
        ]),
```

검증: 결과 화면에서 좌상단을 5번 빠르게 탭 → 어트랙트로 돌아간다. 4번만 탭하고 4초 뒤
1번 더 탭 → **돌아가지 않는다**.

- [ ] **Step 8: 커밋**

```bash
git add apps/booth
git commit -m "feat(booth): 세션 관리 · 어트랙트/인트로/마무리 · 무동작 60초 초기화 · 운영자 제스처"
```

---

## Task 7: 기록 보기 [5-a]

**Files:**
- Create: `apps/booth/lib/widgets/trend_chart.dart`, `apps/booth/lib/screens/history_screen.dart`
- Create: `apps/booth/test/trend_chart_test.dart`

**Interfaces:**
- Consumes: `BoothSession.chartScores`, `BoothSession.seedScores`
- Produces:
  - `TrendChart({required List<int> scores, required int seedCount})` — `seedCount` 이전 점은 회색("예시 기록"), 이후는 티어 색
  - `HistoryScreen({required BoothSession session, required VoidCallback onBack})`
  - `RiskTier riskTierFromScore(int score)` (`risk_tier.dart`에 추가)

- [ ] **Step 1: 실패하는 테스트를 쓴다**

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:helpbee_booth/widgets/trend_chart.dart';

void main() {
  testWidgets('점이 없어도 1개여도 예외 없이 그린다', (tester) async {
    for (final scores in [<int>[], [50], [12, 28, 21, 45, 90]]) {
      await tester.pumpWidget(MaterialApp(
        home: Scaffold(
          body: SizedBox(
            width: 600, height: 300,
            child: TrendChart(scores: scores, seedCount: 4),
          ),
        ),
      ));
      expect(tester.takeException(), isNull);
    }
  });

  testWidgets('예시 기록 표기가 붙는다', (tester) async {
    await tester.pumpWidget(const MaterialApp(
      home: Scaffold(
        body: SizedBox(
          width: 600, height: 300,
          child: TrendChart(scores: [12, 28, 21, 45], seedCount: 4),
        ),
      ),
    ));
    expect(find.text('예시 기록'), findsOneWidget);
  });
}
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd apps/booth && flutter test test/trend_chart_test.dart
```
Expected: FAIL — `trend_chart.dart` 없음

- [ ] **Step 3: `CustomPainter`로 꺾은선을 구현한다**

`apps/booth/lib/widgets/trend_chart.dart`:

```dart
import 'package:flutter/material.dart';

import '../data/risk_tier.dart';
import '../theme/app_colors.dart';

/// 위험도 추이 꺾은선. y축은 0~100 고정(자동 스케일 금지 — 점이 1~2개일 때
/// 축이 요동쳐 아무 의미 없는 그래프가 된다). seedCount 이전은 예시 기록이라
/// 회색으로 그린다.
class TrendChart extends StatelessWidget {
  const TrendChart({super.key, required this.scores, required this.seedCount});

  final List<int> scores;
  final int seedCount;

  @override
  Widget build(BuildContext context) => Stack(
        children: [
          Positioned.fill(
            child: CustomPaint(
              painter: _TrendPainter(scores: scores, seedCount: seedCount),
            ),
          ),
          const Positioned(
            left: 8,
            top: 8,
            child: Text('예시 기록', style: TextStyle(fontSize: 16, color: Colors.black54)),
          ),
        ],
      );
}

class _TrendPainter extends CustomPainter {
  _TrendPainter({required this.scores, required this.seedCount});

  final List<int> scores;
  final int seedCount;

  @override
  void paint(Canvas canvas, Size size) {
    final axis = Paint()
      ..color = Colors.black12
      ..strokeWidth = 2;
    canvas.drawLine(Offset(0, size.height), Offset(size.width, size.height), axis);

    if (scores.isEmpty) return;

    Offset at(int i) {
      final dx = scores.length == 1
          ? size.width / 2
          : size.width * i / (scores.length - 1);
      return Offset(dx, size.height * (1 - scores[i].clamp(0, 100) / 100));
    }

    for (var i = 0; i < scores.length - 1; i++) {
      final isSeed = i + 1 < seedCount;
      canvas.drawLine(
        at(i),
        at(i + 1),
        Paint()
          ..color = isSeed
              ? AppColors.tierUnknown
              : riskTierColor(riskTierFromScore(scores[i + 1]))
          ..strokeWidth = 5
          ..strokeCap = StrokeCap.round,
      );
    }

    for (var i = 0; i < scores.length; i++) {
      final isSeed = i < seedCount;
      canvas.drawCircle(
        at(i),
        isSeed ? 6 : 10,
        Paint()
          ..color = isSeed
              ? AppColors.tierUnknown
              : riskTierColor(riskTierFromScore(scores[i])),
      );
    }
  }

  @override
  bool shouldRepaint(_TrendPainter old) =>
      old.scores != scores || old.seedCount != seedCount;
}
```

`riskTierFromScore(int)`가 `risk_tier.dart`에 필요하다 — Task 2에서 만들지 않았으므로
여기서 추가한다 (apps/mobile 원본과 같은 밴드):

```dart
/// 점수만 있을 때의 티어 (<30 안전 / <70 주의 / 그 외 위험). risk.yaml 밴드와 같다.
RiskTier riskTierFromScore(int score) =>
    score < 30 ? RiskTier.safe : (score < 70 ? RiskTier.watch : RiskTier.danger);
```

- [ ] **Step 4: `history_screen.dart`를 만들고 결과 화면의 `[기록 보기]`에 연결한다**

- [ ] **Step 5: 테스트가 통과하는지 확인한다**

```bash
cd apps/booth && flutter test
```
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add apps/booth
git commit -m "feat(booth): 기록 보기 화면 + 추세 그래프"
```

---

## Task 8: iPad 최적화 · 실물 검증

**Files:**
- Modify: `apps/booth/ios/Runner/Info.plist`
- Modify: `apps/booth/lib/screens/*.dart` (여백·글자 크기 조정)
- Create: `docs/04-operation/booth-runbook.md`

- [ ] **Step 1: `Info.plist`에서 세로 방향을 제거한다**

`UISupportedInterfaceOrientations~ipad`를 아래로 바꾼다 (`Portrait` / `PortraitUpsideDown` 제거):

```xml
<key>UISupportedInterfaceOrientations~ipad</key>
<array>
    <string>UIInterfaceOrientationLandscapeLeft</string>
    <string>UIInterfaceOrientationLandscapeRight</string>
</array>
```

- [ ] **Step 2: 시뮬레이터에서 레이아웃을 맞춘다**

```bash
cd apps/booth && flutter run -d "iPad Air 13-inch (M3)"
```
1366×1024 기준으로 여백·글자 크기를 조정한다. 확인: 본문 22pt 이상 · 위험도 숫자 96pt · 버튼 높이 64pt 이상.

- [ ] **Step 3: 실물 아이패드에 설치한다**

Apple 개발자 계정으로 서명 후:
```bash
cd apps/booth && flutter devices          # 실물 iPad 연결 확인
flutter run -d <ipad-device-id> --release
```

- [ ] **Step 4: 실물에서 체크리스트를 통과시킨다**

- [ ] 가로에서 세로로 돌려도 화면이 안 돌아간다
- [ ] 사진 4장이 밝게 보이고 벌이 알아보인다
- [ ] 박스가 벌 위에 정확히 얹힌다 (사진 3장 전부)
- [ ] QR이 다른 폰으로 스캔된다
- [ ] 60초 방치 → 어트랙트 복귀
- [ ] 안내 접근(Guided Access)으로 앱 이탈이 막힌다
- [ ] 밝기 최대 + 자동 잠금 끔 상태로 **3시간 연속** 구동 후에도 살아 있다

- [ ] **Step 5: 운영 런북을 쓴다**

`docs/04-operation/booth-runbook.md` — 부스 당일 절차: 아이패드 설정(안내 접근·자동 잠금·저전력·밝기), 앱 실행, 이상 시 좌상단 5회 탭으로 강제 초기화, 웹 백업 URL, 응대 시 하지 말아야 할 말(정확도 단정 금지 — `plans/2026-08-12_시연-영상-계획.md` §5).

- [ ] **Step 6: 커밋**

```bash
git add apps/booth/ios docs/04-operation/booth-runbook.md
git commit -m "feat(booth): iPad 가로 고정 + 레이아웃 최적화 + 운영 런북"
```

---

## Task 9: 웹 백업

**Files:**
- Create: `apps/booth/web/robots.txt`
- Create: `apps/booth/vercel.json`

- [ ] **Step 1: 웹으로 빌드한다**

```bash
cd apps/booth && flutter build web --release
```
Expected: 성공. 실패하면 원인이 **외부 패키지**일 것이므로 해당 의존성을 제거한다 (Global Constraints).

- [ ] **Step 2: 로컬에서 확인한다**

```bash
cd apps/booth/build/web && python3 -m http.server 8088
```
아이패드 사파리로 접속 → 홈 화면에 추가 → 전체화면으로 한 바퀴 돌려본다.
**폰트가 시스템 폰트로 떨어지지 않는지** 확인한다 (번들 TTF가 적용됐는지).

- [ ] **Step 3: 검색 노출을 막는다**

`apps/booth/web/robots.txt`:
```
User-agent: *
Disallow: /
```

AI Hub 원본이 공개 URL에 올라가므로 **추측 불가한 경로**로 배포한다 (설계 §11).

- [ ] **Step 4: 배포하고 아이패드로 접속한다**

```bash
cd apps/booth && npx vercel deploy --prod build/web
```

- [ ] **Step 5: 커밋 후 PR을 연다**

```bash
git add apps/booth/web/robots.txt apps/booth/vercel.json
git commit -m "feat(booth): 웹 백업 빌드 + 검색 차단"
git push origin feature/booth-demo-app
```
PR: `feature/booth-demo-app` → `develop`
